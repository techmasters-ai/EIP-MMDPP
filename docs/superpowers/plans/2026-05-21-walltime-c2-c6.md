# Wall-time Reduction Plan — C1.5 + C2 through C6

> **For agentic workers:** REQUIRED: Use `superpowers-extended-cc:subagent-driven-development` (if subagents available) or `superpowers-extended-cc:executing-plans` to execute this plan. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Cut graph-extraction wall time of the air_defense_v3 pipeline against the `bdde417` baseline by ≥50% across two test documents (SA-2 11MB / Dvina 2.3MB) without regressing any of the 10 quality gates from [[extraction-baseline-bdde417]] or the 33-test SA-2+Dvina acceptance harness.

**Two baselines — kept separate** (rev 12):

- **`bdde417` (historical, pre-C1.6 parallel dispatch)** — Dvina ~17.5 min, SA-2 ~5h03m. The reference for: (a) the program-level ≥50% wall reduction; (b) all 15 quality gates (extraction-quality, NOT wall-time).
- **`c16d3e7` / C.0 (active architectural baseline post-C1.6)** — Dvina **52.9 min** (see [[extraction-baseline-c16d3e7]]). The reference for **VR incremental measurement** — what does chunk-narrowing add on top of the C1.6 phase-serialization architecture currently in production?

**Three-layer acceptance for VR** (rev 12 supersedes the rev 10 single-gate framing):

1. **VR incremental gate — PROMOTABLE BAR.** C.7 (Dvina narrow_only) and C.8 (two-doc) compare against **C.0 post-C1.6**, NOT `bdde417`. Field-group `batch_count` ≥30% lower than C.0; field-group long-pole wall materially shorter than C.0's. This is the gate VR must clear to ship to main.
2. **Historical recovery gate — REVIEW BAR.** Total wall on each doc should recover MOST of the C1.6 serialization tax versus `bdde417`. Not a hard gate (Ollama variance dominates absolute wall) but informs Layer 3.
3. **Original program goal — FUTURE BAR.** ≥50% total wall vs `bdde417`. VR alone unlikely to clear this without C5 (input prep cache) + C6 (merge cleanup) — both currently DEFERRED. VR is the **first promotable phase** toward Layer 3, not the final one.

**C1.6 justification check** (NEW per rev 12): C1.6 phase-serialization is wall-time-behavioral. Its cost is only justified if VR substantially shrinks field-group passes. If C.8 shows modest narrowing (batch_count drops <30% OR field-group wall barely budges), C1.6's cost is NOT recouped and **identity-first phase scheduling is a net regression** — surface a C1.6 rollback discussion before main-promotion.

**Architecture:** Incrementally stacked commits on `walltime/c0-telemetry`. **Shipped:** C0 telemetry (`3e01a0b`), C1 concurrency-budget warning (`98daaa3`), P6 library_log trim (`e823abd`), C1.5 dispatcher robustness (`b5b897e`+follow-ups), C1.6 phase model (`84f6b12`+follow-ups). **Active plan:** the **VR (Vector Router)** section below — supersedes lexical C3 + C4. Per-pass dispatch is gated by vector retrieval + cross-encoder rerank against an ephemeral ArcadeDB `ExtractionChunk` index, with a worker-side scoped-doc builder. **Post-VR (deferred):** C5 (cache prepared inputs — needs re-design against router-produced chunk_scope) + C6 (merge/import cleanup — independent of VR).

**Tech Stack:** Python 3.11, FastAPI (api container), Celery (worker containers), Postgres (JSONB diagnostics), Redis, ArcadeDB (vector + graph), MinIO, Ollama (gemma4:31b extraction + bge-m3 embedding via 2-host pool), bge-reranker-v2-m3 (loaded by api container), docling-graph library.

**Per-phase rebuild cadence** (rev 8 L9):

| Phase | api | worker / worker-graph | docling-graph | ArcadeDB schema | Postgres migration |
|---|---|---|---|---|---|
| C0 / C1 / P6 (shipped) | restart | restart | rebuild | — | — |
| C1.5 / C1.6 (shipped) | — | restart | — | — | — |
| VR C.1 — schema + filters | restart | restart | — | auto via `arcadedb_schema.py` | — |
| VR C.2 — index helpers | restart | restart | — | — | — |
| VR C.2b — RetrievalProfile + query builder + manifest defaults (rev 9 H4: owns ontology_bundles.py + both manifests) | **restart api** (manifest pydantic re-import) | restart | — | — | — |
| VR C.3 — endpoint | **rebuild api** (new module imports) + restart | — | — | — | — |
| VR C.4 — worker wiring + scoped-doc builder + janitor | — | restart | — | — | — |
| VR C.5 — manifest calibration | — | restart | — | — | — |
| C5 (deferred) / C6 (deferred) | TBD post-VR | TBD post-VR | TBD post-VR | TBD post-VR | TBD post-VR |

**Note:** docling-graph is NOT rebuilt by any VR phase — scoped DoclingDocument construction is worker-side per H1 in rev 7. Per [[feedback-container-rebuild]] + [[feedback-compose-app-bind-mounts]]: worker code lives in `./app/` bind-mount (restart picks up changes); api code lives in `./app/` bind-mount BUT a new module file requires a process restart from the right cwd (the worktree).

---

## Revision history

- **2026-05-21 (rev 12 — two-baseline framing + three-layer acceptance + C1.6 justification check):** C.0 Dvina baseline regression on `c16d3e7` completed at **52.9 min** vs `bdde417` historical baseline of **17.5 min**. Quality intact (all 5 Dvina gates hold; `missile_kinematics` +1 entity vs baseline is a legitimate improvement). User correctly flagged that I had conflated two senses of "behavioral": C1.6 is NOT extraction-quality behavioral (no entity-count change) but IS absolutely wall-time behavioral — phase serialization replaces parallel dispatch and adds ~25-30 min of wall time on Dvina under current Ollama conditions. Rev 12 corrects the framing:

    - **Two distinct baselines** — kept separate so VR results are interpretable:
      - **`bdde417` (historical pre-C1.6)**: 5-pass Dvina ~17.5 min, 5-pass SA-2 ~5h03m. Useful for: (a) the original program-level ≥50% wall-reduction goal; (b) all 10 SA-2 + 5 Dvina **quality gates** (which are extraction-quality only, NOT wall).
      - **`c16d3e7` / C.0 (active architectural baseline post-C1.6)**: 5-pass Dvina 52.9 min. Useful for: VR incremental measurement — what does narrowing add on top of the current code? See per-pass numbers in `[[extraction-baseline-c16d3e7]]`.

    - **Three explicit acceptance layers** for VR (replaces the single C.8 gate that lumped them together):
      1. **VR incremental gate (PROMOTABLE BAR)**: C.7 (Dvina narrow_only) and C.8 (two-doc) compare against **C.0 post-C1.6**, NOT bdde417. Field-group `batch_count` ≥30% lower than C.0; field-group long-pole wall materially shorter than C.0's. This is the gate VR must clear to ship to main.
      2. **Historical recovery gate (REVIEW BAR)**: total wall on each doc must **recover most of the C1.6 serialization tax** versus `bdde417`. If total Dvina wall lands closer to 30-40 min than 17.5 min, VR is providing partial recovery; if it stays near 52.9 min, VR isn't pulling its weight against the C1.6 cost. **This is NOT a hard gate** — Ollama variance dominates absolute wall — but it informs Layer 3.
      3. **Original program goal (FUTURE BAR)**: ≥50% total wall reduction vs `bdde417`. VR alone is unlikely to achieve this without C5 (input prep cache) and C6 (merge cleanup) — the post-VR optimization checkpoints currently DEFERRED. Layer 3 remains the headline program target; VR is the FIRST promotable phase toward it, not the final one.

    - **C1.6 justification check** added to C.8 acceptance (NEW): C1.6's serialization tax was JUSTIFIED only if field-group passes become substantially smaller via VR narrowing. If C.8 shows VR narrows only modestly (e.g. batch_count drops <30%, or field-group wall barely budges), **identity-first scheduling is a net regression** and should be reconsidered (rollback C1.6 to parallel dispatch + accept that chunk-narrowing happens without phase-ordering enforcement). C.8 acceptance now explicitly asks: "Is C1.6's phase-serialization cost recouped by VR's batch-count drop?" — not just "Did batch count drop ≥30%?" If answer is NO, surface a C1.6 rollback discussion before main-promotion.

    - **Files updated**: Top-level Goal section now lists both baselines + the three-layer acceptance; VR section status header points readers at both; C.8 phase table entry expanded with all three layers and the C1.6 justification question; new memory `[[extraction-baseline-c16d3e7]]` captures the C.0 numbers for future-session continuity.

- **2026-05-21 (rev 11 — ArcadeDB post-filter HNSW finding + Option (b) fallback):** C.1 implementer ran the rev 8 M8 filter-starvation adversarial test. **Test failed**: ArcadeDB's `vectorNeighbors()` in a subquery followed by WHERE filter is definitively post-filter. Bounded 1-2h investigation of filtered-traversal query forms — all four avenues FAILED:
    1. `SELECT ... FROM ExtractionChunk WHERE pipeline_run_id = :rid ORDER BY vectorSimilarity(embedding, :q) LIMIT :k` — same underlying HNSW + RIDBitsFilter post-skip; no change in starvation behavior.
    2. MATCH syntax with explicit index hints — no filtered-traversal support exposed in the SQL layer for vector indexes.
    3. Native `VECTOR_SIMILARITY(filter=...)` parameter — does not exist in ArcadeDB's vector SQL functions.
    4. Composite HNSW index on `(pipeline_run_id, embedding)` — HNSW indexes are single-property; composite vector indexes are not supported.
    Conclusive evidence: ArcadeDB's own comparison document (`docker/arcadedb/repo/docs/arcadedb-vs-leading-vector-dbms.md`, section "Filterable HNSW", P1 gap) explicitly states: "JVector treats [RIDBitsFilter] as a per-point skip during traversal" — HNSW top-K is global, filter is post-step. This is a documented gap, not a configuration issue.
    Fallback to Option (b): new `search_extraction_chunks()` helper in `app/services/extraction_chunk_search.py` uses over-fetch (`top_k = max(desired_top_n * 10, 500)`, retry once at 2000) + Python post-filter by `pipeline_run_id`. `vector_search(filters=...)` remains in the API for non-vector metadata queries but **MUST NOT** be used for VR vector queries — the filter-starvation test stays as the acceptance gate. Diagnostics added: `ann_top_k_requested`, `post_filter_candidate_count`, `post_filter_retry_count`, `filter_strategy="overfetch_post_filter"`, `short_fetch`. Persisted under `pipeline_pass_outputs.diagnostics_json.router.search` (alongside the rev 8 M10 router block). Integration test updated: `TestOverFetchPostFilterStrategy` added to `tests/integration/test_extraction_chunk_filter_starvation.py` — proves the fallback returns all right-run chunks against the same adversarial dataset; original starvation test preserved as bug-catcher acceptance gate (expected to fail on bare `vector_search(filters=...)`).

- **2026-05-21 (rev 1):** First draft covering C1.5 → C6.
- **2026-05-21 (rev 10 — cleanup pass on rev 9):** User returned 9 findings flagging stale text that an agentic implementer could follow literally + one success-metric mismatch. All real. Resolutions:
    - **H1 — Headline goal vs C.8 gate mismatch**: Plan goal at top states ≥50% wall-time reduction across both docs. C.8 gate as written required only ≥30% batch_count reduction + "same or lower" total wall. **Restated**: VR is the **first promotable phase toward** the ≥50% headline goal — not the final phase. The full ≥50% target requires VR + post-VR optimization checkpoints (C5/C6, deferred). **C.8 gate (rev 10)**: (a) all 5 Dvina + 10 SA-2 quality gates hold; (b) SA-2 field-group `batch_count` ≥30% lower than baseline; (c) total SA-2 wall ≤ baseline (no regression); (d) **stretch target — SA-2 total wall ≤ baseline × 0.70 (~30% reduction)**. The stretch target is not a gate failure; missing it triggers a post-VR review of where remaining wall lives (LLM call latency vs prep vs merge), which informs C5/C6 re-design.
    - **H2 — Shadow-mode chunk_scope leakage**: Rev 9 H1 said `_claim_and_dispatch_pass` passes `chunk_scope` as a Celery kwarg + per-pass task applies it if `mode=selected_refs`. But in shadow mode, the router may return `selected_refs` while the worker MUST dispatch full. Without distinguishing raw router output from effective worker scope, shadow accidentally narrows. **Resolved with `effective_chunk_scope`**:
      ```python
      # in _claim_and_dispatch_pass, AFTER receiving router_response:
      router_diagnostics = router_response.diagnostics  # ALWAYS includes raw router decision
      mode = os.environ.get("VECTOR_ROUTER_MODE", "shadow")
      if mode == "disabled":
          effective_chunk_scope = None
      elif mode == "shadow":
          effective_chunk_scope = None  # NEVER narrow in shadow
          router_diagnostics["shadow_skipped_narrowing"] = (
              router_response.mode == "selected_refs"
          )
      elif mode == "narrow_only":
          if router_response.mode == "selected_refs":
              effective_chunk_scope = router_response.chunk_scope  # narrow
          else:  # mode in (full, would_skip)
              effective_chunk_scope = None  # fail open / full
              if router_response.mode == "would_skip":
                  router_diagnostics["fail_open_reason"] = "would_skip_in_narrow_only_mode"
      # else: skip_enforce DEFERRED, no code path
      derive_ontology_graph_pass.delay(
          document_id, run_id, pass_name,
          chunk_scope=effective_chunk_scope,       # what worker actually applies
          router_diagnostics=router_diagnostics,    # what router decided (raw)
      )
      ```
      Per-pass task body: `if chunk_scope and chunk_scope.get("mode") == "selected_refs": apply_chunk_scope(doc, chunk_scope)`. Otherwise full doc. **Shadow + narrow_only are now correctly orthogonal: shadow always full; narrow_only narrows on selected_refs, fails open otherwise.**
    - **H3 — Janitor cross-store contradiction in active text**: Rev 9 H3 specified the worker algorithm in the revision history, but the active VR architecture diagram + ExtractionChunk schema comment still showed cross-DB SQL referencing `pipeline_run.status` from inside an ArcadeDB delete. **Replaced** all active inline pseudo-SQL with a pointer to the worker algorithm: "see rev 9 H3 worker algorithm — query ArcadeDB for old run_ids → fetch statuses from Postgres → compute purge set → bulk DELETE from ArcadeDB. NEVER a single cross-store SQL statement."
    - **M4 — Recall gate wording inconsistent**: Rev 9 H2 introduced the critical-evidence-only gate but legacy "`ChunkScope.self_refs ⊇ baseline_evidence_refs` per pass" wording remained. **Single canonical wording (rev 10)**: "C.2b deterministic gate asserts **`set(pre_rerank_top_n_candidates) ⊇ set(normalized_critical_evidence_refs)`** where critical = (a) 100% Dvina baseline evidence refs across all 5 passes, AND (b) ONLY SA-2 baseline evidence refs for entities counted by quality gates #5 (`emitter_function` 21/34) and #6 (`min_altitude_km` 9/44). Other SA-2 provenance is informational."
    - **M5 — Pre-rerank recall ≠ final scope recall**: The C.2b deterministic gate validates VECTOR recall only; the reranker could subsequently drop a critical chunk and the load-bearing test would still pass. **Added C.6 post-rerank gate**: C.6 shadow run must report **per-pass post-rerank critical evidence coverage** via the persisted `pipeline_pass_outputs.diagnostics_json.router` data. New explicit C.6 acceptance: "for every quality-gated pass on both Dvina and SA-2, the persisted `selected_refs` (final post-rerank set, NOT pre-rerank candidates) must cover the same normalized critical evidence refs the C.2b deterministic gate asserted." If reranker dropped a critical chunk, FAIL C.6 and tune before proceeding to C.7.
    - **M6 — `fallback_to_full: true` masks would_skip behavior**: With the conservative default, the endpoint NEVER returns `mode=would_skip` — so C.6 can't actually validate the would-skip branch. **Added `would_skip_if_fallback_disabled` diagnostic** to router response: when empty-after-threshold AND `fallback_to_full=true`, endpoint returns `mode=full` (current behavior) BUT diagnostic includes `would_skip_if_fallback_disabled=true`. Lets operator inspect which passes would have skipped without actually skipping. C.6 acceptance updated: "log any pass with `would_skip_if_fallback_disabled=true` on Dvina (relevant doc) — flagged as recalibration trigger BEFORE C.7."
    - **M7 — `scoped_docling_document.py` missing + backward-compat test missing**: Both added:
      - Files table gets new row: `app/services/scoped_docling_document.py` | NEW (C.4) — `apply_chunk_scope(doc_json: dict, chunk_scope: dict) -> dict`. Preserves arrays; rewrites body.children; parent-ref validation.
      - C.4 test added: `test_per_pass_task_backward_compat_signature` — derive_ontology_graph_pass.delay(...) called WITHOUT `chunk_scope` / `router_diagnostics` kwargs (old queued tasks; pre-VR Celery messages still in broker after rollout) must run full-doc cleanly. Validates Python kwarg defaults are `None` and `apply_chunk_scope` is conditionally called.
    - **M8 — SUPERSEDED C3/C4 sections retain live checkboxes**: Wrapped C3 and C4 sections in `<details>` blocks (matching C2 treatment in rev 7 L12). Each gets a top-level "**DO NOT IMPLEMENT** — see VR section for active plan" notice. Checkboxes inside historical content are agnostic now — even if an agentic worker stumbles into them, the wrapping clearly marks them as not active.
    - **L9 — Stale terminology sweep**: Three sites updated:
      - "Q6: Dvina first (cheap iteration loop), SA-2 only after Dvina shadow + enforce both pass" → "Dvina first (C.6 shadow + C.7 narrow_only smoke), SA-2 only in C.8 promotable two-doc regression."
      - "Post-C6 merge cleanup" framing in the VR section + status block: removed references to C3/C4/C5/C6 as active. C5 + C6 are explicitly "REVIEW REQUIRED POST-VR."
      - "C.5 still owns retrieval blocks" mentions: cleaned per rev 9 H4 — C.5 is calibration-only, C.2b owns the schema + defaults.

- **2026-05-21 (rev 9 — incorporate user's 6th independent review on rev 8):** User returned 9 findings on rev 8 (4 HIGH, 4 MED, 2 LOW). All are real stale/contradiction gaps. Resolutions:
    - **H1 — Selected-scope handoff into per-pass Celery task**: Rev 8 specified router_diagnostics flowing through to terminal save, but missed the actual narrowing handoff. **Diagnostics don't narrow extraction** — only the `chunk_scope` payload does. Specified flow in C.4:
      - `_claim_and_dispatch_pass` receives `{mode, self_refs, ...}` (the `chunk_scope` JSON dict — small, kilobytes) from `/v1/extraction/chunk-scope`.
      - Dispatch: `derive_ontology_graph_pass.delay(document_id, run_id, pass_name, chunk_scope=<dict>, router_diagnostics=<dict>)`.
      - Per-pass task body: loads full doc from MinIO (already does this — line 7247-ish). If `chunk_scope and chunk_scope.get("mode") == "selected_refs"`: call `apply_chunk_scope(doc_json, chunk_scope)` **inside the task body** before `_execute_pass_attempt`. If `mode` in (`full`, `would_skip` + narrow_only fail-open): use full doc.
      - **NEVER pass the scoped doc JSON itself through Celery** — it's potentially megabytes. Only the small `chunk_scope` dict (a few hundred bytes) crosses the broker.
      - `_save_terminal_pass_output` merges `router_diagnostics` into `pipeline_pass_outputs.diagnostics_json.router` (unchanged from rev 8 M7).
    - **H2 — Offline-recall gate impossible if baseline evidence count > top_k**: SA-2's `radar_power_rf` produced 34 entities with potentially 60+ evidence refs in baseline. With `top_k=20`, asserting `selected_refs ⊇ baseline_evidence_refs` is impossible by construction. Replaced with **critical-evidence-only gate**:
      - **Dvina (5 passes, manageable cardinality)**: 100% baseline evidence ref recall required. Each pass produces 1-2 entities with ~5-15 evidence refs total per doc — all should be in scope.
      - **SA-2 (10 passes, high cardinality)**: ONLY evidence refs for the entities counted by **gates #5 (`emitter_function` 21/34)** and **#6 (`min_altitude_km` 9/44)** must be in scope. These are the gates most sensitive to narrowing per [[extraction-baseline-bdde417]]. The OTHER SA-2 baseline provenance is informational, not blocking.
      - Helper: `_extract_critical_evidence_refs(baseline_outputs, gates)` lives in the deterministic test. Documents the gate→entity→evidence_id chain explicitly.
    - **H3 — Janitor SQL is cross-database**: `ExtractionChunk` in ArcadeDB; `pipeline_runs.status` in Postgres. A single ArcadeDB DELETE cannot reference Postgres state. Janitor is a worker algorithm:
      ```python
      async def purge_terminated_extraction_chunks():
          # 1. ArcadeDB: distinct pipeline_run_ids with old chunks
          old_run_ids = await arcadedb.query_distinct(
              "ExtractionChunk", "pipeline_run_id",
              where="created_at < $1::TIMESTAMP - INTERVAL '24' HOUR",
          )
          # 2. Postgres: batched status lookup
          pg_rows = await postgres.fetch_run_statuses(old_run_ids)
          pg_statuses = {r.id: r.status for r in pg_rows}
          # 3. Compute purge set: terminal OR orphan (Postgres has no record)
          to_purge = [
              rid for rid in old_run_ids
              if pg_statuses.get(rid) in ("COMPLETE", "FAILED", "PARTIAL_COMPLETE")
              or pg_statuses.get(rid) is None
          ]
          # 4. ArcadeDB: bulk delete
          n = await arcadedb.delete_where(
              "ExtractionChunk", f"pipeline_run_id IN ({','.join(repr(r) for r in to_purge)})",
          )
          logger.info("janitor: purged %d chunks across %d runs", n, len(to_purge))
          return {"purged_runs": len(to_purge), "purged_chunks": n}
      ```
      Lives in `app/workers/pipeline.py:purge_terminated_extraction_chunks` (new Celery task). Beat schedule: hourly.
    - **H4 — C.2b/C.5 ownership contradictory**: Rev 8 said C.2b ships `RetrievalProfile` and default manifest `retrieval:` blocks. Rev 8's files table and tests still listed those under C.5. **Resolved**: move `app/services/ontology_bundles.py` (PassManifest + RetrievalProfile) AND `ontology_bundles/air_defense_v3/manifest.yaml` + `ontology_bundles/air_defense_v3_baseline_subset/manifest.yaml` (with `retrieval:` blocks containing conservative defaults) into **C.2b's files list**. **C.5 = manifest threshold-value calibration ONLY** — no schema work, no new code, just edits to `min_similarity` / `top_k` / `fallback_to_full` per-pass values based on C.6 shadow data.
    - **M5 — VECTOR_ROUTER_MODE values inconsistent**: Sweep all references to `(disabled/shadow/enforce)` and replace with `(disabled/shadow/narrow_only)`. `skip_enforce` listed only as DEFERRED, never as an available value. Updated in: VR Files table, `.env`/`.env.example` comment, env-var description, C.4 task list.
    - **M6 — C.4 tests still say "skip decision writes terminal row"**: That's `skip_enforce` behavior, deferred. Replace test with "**would_skip in narrow_only mode** → worker dispatches RUN_FULL + persists `diagnostics_json.router.fail_open_reason='would_skip_in_narrow_only_mode'`." Terminal SKIPPED row test moves to deferred skip_enforce future plan (not in active chain).
    - **M7 — Mock reranker in load-bearing test misrepresents production**: Restructured the C.2b offline-recall split:
      - **Deterministic gate (`test_vector_router_offline_recall_deterministic.py`)**: validates **vector recall PRE-RERANK**. Uses precomputed bge-m3 chunk embeddings + bge-m3 query embedding. Computes top-`top_n_candidates` (e.g. 50) by cosine. Asserts critical evidence refs (per H2 normalization) appear in this pre-rerank candidate set. No reranker involvement. Truly hermetic; runs in seconds. **This is the load-bearing gate** — if vector retrieval misses an evidence chunk, no reranker can save it.
      - **Live smoke test (`tests/integration/test_vector_router_live_recall.py`)**: validates full pipeline including rerank against live `embed_texts()` + `app.services.reranker.rerank()`. Skip-if-Ollama-or-reranker-unavailable. Captures real-world drift end-to-end. NOT load-bearing — drift in rerank ordering is a calibration signal, not a correctness failure.
    - **M8 — Regression cadence: Dvina-only smoke vs full-two-doc-promotable**: Updated main "Regression test cadence" section with explicit gate:
      - **C.6 (shadow) and C.7 (Dvina narrow_only)**: Dvina-only regression run. May commit **calibration-only changes** (threshold tuning in manifest yaml) after the Dvina smoke passes + diagnostics are reviewed. NOT promotable to main.
      - **C.8 (two-doc narrow_only Dvina + SA-2)**: Full regression. Required for any main promotion. All 5 Dvina + 10 SA-2 quality gates must hold; SA-2 field-group batch_count ≥30% lower than baseline.
      - **Implementation work for C.1-C.5**: TDD + 33-test acceptance harness — no live LLM regression required for incremental commits within these phases.
    - **L9 — 5 Dvina quality gates not enumerated**: Added Dvina gate list adjacent to the SA-2 10-gate enumeration in the "Quality gates" section:
      1. `radar_identity`: 1 RADAR_SYSTEM entity (SNR-75)
      2. `radar_power_rf`: 1 RADAR_SYSTEM entity with ≥1 RF field populated
      3. `missile_identity`: 1 MISSILE_SYSTEM entity (SA-2 family)
      4. `missile_kinematics`: 2 MISSILE_SYSTEM entities (variants) with ≥1 kinematics field each
      5. `system_links`: 1 ASSOCIATED_WITH relationship (radar↔missile)
    - **L10 — C6 actionable but depends on deferred C4a catalog**: Marked C6 section with same "**REVIEW REQUIRED POST-VR**" notice as C5. C6's catalog serializer was originally co-hosted with C4a's identity roster helper. With C4a deferred (rev 5 Q4 → schema-only queries), C6 needs to either: (a) build its own catalog serializer that doesn't depend on C4a's roster; (b) un-defer C4a as a C6 prerequisite. Both options are deferred until VR ships and C5/C6 are re-evaluated.

- **2026-05-21 (rev 8 — incorporate user's 5th independent review on rev 7):** User returned 9 findings on rev 7 (3 HIGH, 5 MED, 2 LOW). All are stale-instruction gaps that an agentic worker would follow literally. Resolutions:
    - **H1 — C.5 ordering contradiction**: C.6 (shadow regression) requires retrieval config to run, but rev 7 said C.5 calibration happens AFTER C.6. Resolved: **C.2b SHIPS the manifest `retrieval:` blocks with CONSERVATIVE DEFAULTS** in both `air_defense_v3/manifest.yaml` and `air_defense_v3_baseline_subset/manifest.yaml` — same `min_similarity=0.45`, `top_n_candidates=50`, `top_k=20`, `fallback_to_full=true` (conservative = fail-open on no matches) on every field-group pass. C.6 runs against these defaults. C.5 becomes "**Manifest calibration UPDATE** after C.6 shadow logs are reviewed" — fills in per-pass tuned thresholds if shadow data shows specific passes need adjustment.
    - **H2 — Offline-recall gate evidence-ref normalization**: Baseline `field_provenance.evidence_id` may point to refs that aren't the docling self_ref granularity used by `ExtractionChunk`. Possible mismatches: cell-level refs (`#/tables/3/cells/0`), page-level refs, group refs (`#/groups/N`), or sanitized/normalized table cell refs. The C.2b offline-recall test must NORMALIZE before assertion:
      - Cell-level evidence (`#/tables/N/cells/K`) → normalize to parent table ref (`#/tables/N`).
      - Page-level evidence → normalize to all `#/texts/M` / `#/tables/M` / `#/pictures/M` elements on that page.
      - Group refs (`#/groups/N`) → normalize to constituent texts/tables.
      - Then assert `set(chunk_scope.self_refs) ⊇ set(normalize(baseline_evidence_refs))`.
      Helper `_normalize_evidence_refs(doc, refs) -> set[str]` lives in `tests/integration/test_vector_router_offline_recall.py`. Documents the normalization rules explicitly.
    - **H3 — "Hermetic offline recall" conflicts with live embed/rerank**: Split into TWO tests at C.2b:
      - **`test_vector_router_offline_recall_deterministic.py`** (hermetic, MUST PASS to merge any VR commit). Uses precomputed fixtures: chunk embeddings stored as a fixture file per doc (generated once via a one-time helper script `scripts/generate_baseline_recall_fixtures.py`), canned rerank scores derived from a deterministic scoring function (e.g. cosine of query vs chunk; OR mocked reranker that returns identity-mapped scores). NO live calls. Runs in <5s. This is the load-bearing gate.
      - **`tests/integration/test_vector_router_live_recall.py`** (integration, skip-if-Ollama-or-reranker-unavailable). Uses live `embed_texts()` + `app.services.reranker.rerank()`. Same normalization + coverage assertion. Captures real-world calibration drift. Not load-bearing; explicit smoke test.
    - **M4 — Stale contradictions on `skip` vs `would_skip` + reranker fallback**: Endpoint output renamed:
      - `mode=selected_refs` — router selected K refs, worker should narrow
      - `mode=full` — no narrowing (either no matches AND fallback_to_full=true, OR identity/required pass)
      - **`mode=would_skip` — router believes no relevant chunks; WORKER decides what to do based on `VECTOR_ROUTER_MODE`**. In `narrow_only`, worker fails open to RUN_FULL with diagnostic. In future `skip_enforce` (deferred), worker would write terminal SKIPPED row.
      The endpoint NEVER decides "skip" unilaterally — that's a worker-level decision keyed on mode. Three contradiction sites fixed in this rev: (1) VR architecture diagram, (2) manifest example `fallback_to_full` note, (3) C.3 test list ("reranker fallback to vector-only" replaced with "reranker error → mode=full"). The `fallback_to_full` manifest field stays — it controls whether NO MATCHES returns `mode=full` (true) vs `mode=would_skip` (false). Worker handles the latter per mode.
    - **M5 — ExtractionChunk schema omits `created_at`; janitor description stale**: VR schema visible block updated to include `("created_at", "TIMESTAMP")` with default NOW() + b-tree index. Janitor description updated to rev-7 24h-and-orphan rule: `DELETE WHERE created_at < NOW() - INTERVAL '24 hours' AND (pipeline_run is NULL OR pipeline_run.status IN ('COMPLETE','FAILED','PARTIAL_COMPLETE'))`. Captures both terminal-run leftovers AND orphan rows.
    - **M6 — Relationship passes (`system_links`) never narrowed**: Updated never-narrowed list to include all three: **identity passes, required passes, AND relationship passes**. Specifically `system_links` stays full-doc/full-upstream regardless of VECTOR_ROUTER_MODE. Narrowing relationship-pass input could break composition gates (a relationship between entity A and entity B requires BOTH entities' evidence to be in scope; narrowing risks one being missed). Narrowing relationship passes is **deferred indefinitely** — a separate future plan with its own composition quality gates.
    - **M7 — Router diagnostics must flow to terminal pass_output**: Specified the dispatch-to-terminal flow:
      - `_claim_and_dispatch_pass` calls `/v1/extraction/chunk-scope`, receives `router_diagnostics: dict`.
      - Diagnostics dict passed as kwarg to the Celery task: `derive_ontology_graph_pass.delay(document_id, run_id, pass_name, router_diagnostics=router_diagnostics)`.
      - Per-pass task body carries it through `_execute_pass_attempt` → `PassAttemptOutcome`.
      - `_save_terminal_pass_output` merges `router_diagnostics` into `pipeline_pass_outputs.diagnostics_json` under key `router`. Same pattern as the existing C0 `worker_diagnostics` merge.
      - Updated `_build_pass_diagnostics_dict()` accepts the optional router dict.
      - Backward-compat: when `router_diagnostics=None` (e.g. identity/required pass bypass), the `router` key is omitted from diagnostics. No breaking change for existing fields.
    - **M8 — `selected_token_estimate` validation**: Endpoint computes `selected_token_estimate` (sum of token counts of selected chunks) AND `full_doc_token_estimate` (sum of all chunks). Both returned in `router_diagnostics`. Worker logs WARNING if `selected_token_estimate / full_doc_token_estimate > 0.80` — narrowing isn't pulling its weight on this pass; threshold may be too generous. Pass still proceeds with the narrowed scope (no fail-open trigger); the WARNING surfaces the calibration issue for the operator without breaking the dispatch.
    - **L9 — Top-level framing stale**: Two updates: (1) Architecture line at the top of the plan now reads "C0/C1/P6 commits already shipped; C1.5 + C1.6 phase model already shipped; **VR (Vector Router) supersedes C3 + C4 lexical** — see VR section below for active architecture. C5 + C6 are post-VR optimization checkpoints pending re-evaluation against VR's worker-side scoped-doc construction." (2) Rebuild cadence in main cadence section updated to include VR phases: VR C.1-C.5 require **api rebuild + worker bind-mount restart**; **NO docling-graph rebuild** (scoped doc built worker-side); **ArcadeDB schema sync auto** via `arcadedb_schema.py` registration on api/worker startup; **NO postgres migration** (router diagnostics live in existing `diagnostics_json`).
    - **L10 — C5 cache section refers to "C4 chunk_scope" + docling-graph cache**: C5 section now has a **REVIEW POST-VR** notice at top. C5's Layer A cache (full sanitized base doc) is doc-keyed and still works post-VR. C5's Layer B cache (pass-specific) was keyed on `chunk_scope_hash` — that key changes because chunk_scope is now produced by the VR router, not the manifest. C5 needs re-evaluation after C.8 ships. **C5 is removed from the active phase chain until that re-eval.**
- **2026-05-21 (rev 7 — incorporate user's 4th independent review):** User returned 13 findings on rev 6 (4 HIGH, 6 MED, 2 LOW + structural plan-change proposals). Two HIGH findings exposed gaps the prior three reviewers missed: (1) mode staging — shipping SKIP + narrowing together in first enforce rollout risks zeroing a quality-gated pass on one bad threshold; (2) deterministic offline recall validation before live LLM runs prevents wasting SA-2 hours on a router that loses required evidence. Rev 7 resolutions:
    - **H1 — Scoped-doc application path**: VR docs claimed C4d was rolled into C.4 but did not specify HOW the scoped doc gets built. Rev 7 adds new helper `app/services/scoped_docling_document.py:apply_chunk_scope(doc_json: dict, chunk_scope: ChunkScope) -> dict`. Preserves `texts[]`/`tables[]`/`pictures[]` array positions exactly (per the C0-era sanitize precedent); rewrites only `body.children` traversal to walk the selected `self_refs`; validates that every retained child's `parent` resolves to a reachable ancestor in the rewritten body. Tests in C.4: array-position invariance, parent-ref resolution, `/extract-pass` round-trip with worker-built scoped doc consumed correctly.
    - **H2 — Mode staging (shipping SKIP + narrowing together is unsafe)**: Mode taxonomy revised:
      - `disabled` — router not invoked (kill switch).
      - `shadow` — router runs and persists decisions to `pipeline_pass_outputs.diagnostics_json.router`, but worker ALWAYS dispatches RUN_FULL regardless. SKIP determinations are diagnostic only.
      - `narrow_only` — router runs and persists decisions; if router returns `selected_refs`, worker applies chunk-scope narrowing; if router returns `would_skip` (no chunks pass threshold), worker FAILS OPEN to RUN_FULL with diagnostic `fail_open_reason=would_skip_in_narrow_only_mode`. **Required and identity passes are NEVER narrowed.**
      - `skip_enforce` — **DEFERRED**, not in the active plan. SKIP enforcement requires an off-topic-doc corpus, explicit false-positive gates, and status/API semantics — separate future plan if production evidence warrants. SKIP decisions stay as diagnostic logs in C.6-C.8.
      - Default on first deploy = `shadow`; promote to `narrow_only` after C.6 shadow validates router decisions; `narrow_only` is the ship target.
    - **H3 — C.4 depends on C.5 config but C.5 is after**: Split C.5 into two phases. New **C.2b — RetrievalProfile + Query Builder + Offline Recall** ships BEFORE C.3 with: `RetrievalProfile` pydantic + manifest defaults, `build_retrieval_query(pass_def, template_cls) -> str` helper, fallback policy locked into the schema, snapshot tests, AND the H4 offline recall test (below). C.5 keeps its name but is now **manifest calibration** — fills in calibrated threshold values once C.6/C.7 shadow data is in. C.5 ships before the C.7 narrow_only regression.
    - **H4 — Deterministic offline recall validation**: New test in C.2b uses bdde417 baseline data: for each quality-gated field-group pass, load the baseline's pass_output entities + their `field_provenance.evidence_id` self_refs. Run the vector router OFFLINE against the same doc with current thresholds. Assert `set(chunk_scope.self_refs) ⊇ set(baseline_evidence_refs)`. If a baseline evidence ref isn't in the selected scope, the test FAILS and the implementer must tune query/threshold before any live regression run. Hermetic — uses fixtures, no LLM, no Ollama; runs in seconds. **This is the new gate** between C.5 calibration and C.7 narrow_only regression.
    - **M5 — Query construction underspecified**: `build_retrieval_query(pass_def, template_cls)` walks the pydantic model's `model_fields[*].description`, joins descriptions with newlines, includes the schema's docstring. Snapshot tests for `radar_power_rf` + `missile_kinematics`. Snapshot becomes the regression guard against query drift.
    - **M6 — Reranker failure → RUN_FULL not vector-only**: C.3 acceptance updated. Reranker exception = infrastructure degradation, not evidence of irrelevance. Endpoint returns `mode=full, reason=reranker_unavailable`. Vector-only ordering is NOT the fallback.
    - **M7 — `created_at` on ExtractionChunk + age-based cleanup**: C.1 schema adds `created_at TIMESTAMP DEFAULT NOW()` indexed. Janitor cleanup rule: `WHERE created_at < NOW() - INTERVAL '24 hours' AND (pipeline_run_id NOT IN (SELECT id FROM ingest.pipeline_runs WHERE status='PROCESSING') OR NOT EXISTS (SELECT 1 FROM ingest.pipeline_runs WHERE id=pipeline_run_id))`. Catches both terminal-run leftovers AND orphan rows whose pipeline_run record was deleted/never created.
    - **M8 — Filter-starvation test**: C.1 adds adversarial test: insert 100 wrong-run `ExtractionChunk` rows whose embeddings are crafted to be globally nearest to a synthetic query, plus 5 right-run rows that are less close. Filtered `vector_search(filters={pipeline_run_id: right_run})` must return all 5 right-run chunks, NOT empty. If ArcadeDB applies the WHERE filter AFTER top-K HNSW (post-filter starvation), this test will fail — and we'd need to switch the implementation to exact KNN under filter OR over-fetch and post-filter at scale. **Test is the load-bearing assertion** that the architecture is sound.
    - **M9 — Chunk text rendering contract**: C.2 adds explicit rendering rules. Text elements: raw `text` field. Tables: caption + cells joined as markdown (`| col1 | col2 |\n|---|---|\n| cell1 | cell2 |`). Picture captions: caption text only (no inferred description). PLUS context expansion options gated by manifest flags: `chunk_render.include_parent_section_heading: bool = true` (prepend parent section heading), `chunk_render.include_table_caption: bool = true` (already in default for tables). Default both true — captures cross-reference signal that exact-words queries would miss.
    - **M10 — Persist router decisions in pipeline_pass_outputs.diagnostics_json**: All router decisions across shadow/narrow_only modes persist via the existing C0 worker_diagnostics merge under key `router`:
      ```json
      {
        "mode": "shadow|narrow_only",
        "decision": "RUN_FULL|selected_refs|would_skip|fail_open_run_full",
        "query_text": "...",
        "vector_threshold": 0.45,
        "vector_score_range": [0.42, 0.71],
        "rerank_score_range": [0.38, 0.94],
        "candidate_count": 50,
        "selected_ref_count": 18,
        "selected_token_estimate": 4400,
        "index_build_ms": 2300,
        "chunk_scope_http_ms": 1800,
        "vector_search_ms": 35,
        "rerank_ms": 1700,
        "fallback_reason": "would_skip_in_narrow_only_mode | reranker_unavailable | index_build_failed | http_timeout | http_5xx | none"
      }
      ```
      No new table needed. Survives via C0 telemetry path.
    - **M11 — Regression cadence: Dvina-only = smoke; two-doc = promotable**: C.6 (shadow) and C.7 (Dvina narrow_only) are now explicitly labeled **calibration/smoke runs** — NOT promotable. **C.8** is the promotable gate: two-doc narrow_only regression on Dvina + SA-2; all 10 SA-2 quality gates + all 5 Dvina quality gates must hold; field-group `batch_count` ≥ 30% lower than baseline on SA-2.
    - **L12 — C2 stale tasks**: C2 section body replaced with RETIRED notice. Phase tasks deleted to prevent stale-instruction execution.
    - **L13 — Rebuild cadence**: New table in Status section: VR phases C.1-C.5 require **api rebuild + worker restart** (api owns the new endpoint; worker bind-mount picks up other changes). No docling-graph rebuild. ArcadeDB schema sync happens automatically via `arcadedb_schema.py` registration. Postgres: no migration needed (router decisions live in existing `pipeline_pass_outputs.diagnostics_json`).
    - **New phase order**: C.0 → C.1 → C.2 → C.2b (NEW) → C.3 → C.4 → C.5 → C.6 → C.7 → C.8. C.9 (`skip_enforce`) deferred.

- **2026-05-21 (rev 6 — incorporate findings from 3 independent plan reviews):** Three independent reviewers (architecture, implementation detail, test coverage / TDD) returned 23 findings against rev 5. Rev 6 addresses all 6 HIGH + 10 MED + 4 LOW findings inline. Key resolutions:
    - **H1 — Dispatcher has no `doc`**: VR phase C.4 task list now explicitly adds `_build_docling_document_json(document_id)` as the first step inside the dispatcher's C1.5 try/except block, BEFORE `build_extraction_index`. The `doc_json` flows in as `dict` (not pydantic `DoclingDocument`).
    - **H2 — HTTP fallback behavior**: C.4 acceptance now specifies: ANY HTTP error (timeout, 5xx, connection refused) from `/v1/extraction/chunk-scope` → log WARNING with `(run_id, pass_name, error)` → proceed as `mode=full` (RUN_FULL). Never propagate to pass failure.
    - **H3 — TDD discipline**: Each VR phase (C.1-C.5) is now expanded into an explicit task list matching the C1.5/C1.6 pattern: discussion gate → write failing test → confirm RED → minimal implementation → confirm GREEN → acceptance harness 33/0 → commit. RED-first is non-negotiable.
    - **H4/H5/H6 — Test coverage gaps**: Each phase gets explicit failure-mode tests:
      - C.1: filter-isolation cross-run test; empty-filter path; `None` filter falls back to unfiltered.
      - C.2: zero-element doc; concurrent-build idempotency; cleanup-scope isolation between runs.
      - C.3: reranker-disabled; empty-after-threshold; malformed query; `rerank()` raises.
      - C.4: HTTP timeout; HTTP 500; partial-build mid-fail; double-cleanup idempotency; janitor 24h logic.
    - **M1 — vector_search filter signature**: C.1 task list now acknowledges the existing `document_ids: list[str] | None` parameter on the concrete `ArcadeDBGraphStore.vector_search` (`app/services/arcadedb_graph.py:1557`). Design decision: ADD `filters: dict[str, Any] | None = None` as a NEW parameter alongside `document_ids` (don't repurpose). Both compile to ArcadeDB WHERE clauses. The Protocol stub at `app/services/graph_store.py:337` must be synced to include both. The plan also adds an audit task to verify no other callers break.
    - **M2 — chunk_text → content_text contract**: VR C.3 endpoint internals must map `ExtractionChunk.chunk_text` → candidate dict `content_text` field before calling `reranker.rerank()` (which filters `c.get("content_text")` at `reranker.py:57`). Added as explicit test assertion.
    - **M3 — sync embed_texts in async endpoint**: VR C.3 endpoint MUST call `embed_texts()` via `loop.run_in_executor(None, _embed)` mirroring the existing pattern at `app/api/v1/retrieval.py:413-424`. Added to C.3 task list.
    - **M4 — sync HTTP from Celery**: Worker calls `/v1/extraction/chunk-scope` via `httpx.Client` (sync), matching the existing pattern for service-call dispatch from `_execute_pass_attempt`. Specified in C.4.
    - **M5 — index build failure recovery**: C.4 wraps `build_extraction_index(...)` in its OWN try/except inside the dispatcher (NOT the C1.5 outer try/except). On failure: log WARNING, set local `_index_built = False`, continue with RUN_FULL semantics for all field-group passes (router becomes pass-through). Plan acceptance updated.
    - **M6 — VECTOR_ROUTER_MODE default**: `shadow` on first deploy. Promotion to `enforce` is the C.7 task. `.env` and `.env.example` mirroring per [[feedback-env-vars-must-appear-in-dotenv-files]].
    - **M7 — calibration smoke test**: New deterministic test in C.5 uses pre-computed Dvina chunk embeddings (fixture) to verify initial `min_similarity=0.45` + `top_k=20` selects a reasonable ChunkScope per pass on Dvina before paying for a live regression iteration.
    - **M8 — hermetic vs integration boundary**: Each test in C.1-C.4 is now annotated as "unit (mocked)" or "integration (requires ArcadeDB / skip-if-no-DB guard)" per the existing `tests/integration/test_dispatcher_post_queue_recovery.py` pattern.
    - **M9 — acceptance harness gate**: Every VR phase's task list ends with "run acceptance harness 33/0" as a mandatory gate before commit, matching the C1.5/C1.6 pattern.
    - **M10 — filter-contradiction tests**: Covered by H4 above.
    - **L1 — embedding dim = 1024**: Explicit in C.1 schema (matches existing TextChunk index dim).
    - **L2 — cleanup failure visibility**: `cleanup_extraction_index` failures logged at WARNING level inside `_terminalize_doc_and_run`. Janitor provides defense-in-depth.
    - **L3 — docling-graph rebuild**: VR does NOT modify `docker/docling-graph/` per the locked design. Worker bind-mount changes only. The "regression test cadence" note remains accurate.
    - **L4 — C.6 expected shadow log pattern**: C.6 acceptance now lists explicit expected log entries: for each Dvina field_group pass, expect `decision=RUN_FULL chunks_with_hits ≥ 5` (since Dvina has all the radar + missile content). Any `decision=skip` on Dvina = recalibration trigger (lower min_similarity).
- **2026-05-21 (rev 5 — vector router replaces C3 + C4 lexical):** User pivoted away from keyword-driven gating after concluding that cue-list matching wouldn't be selective enough for the corpus. New architecture: **VR (Vector Router)** supersedes both C3 (lexical skip router) and C4c (lexical chunk-scope selection). Lexical C3 commit (`db1ea53`) reverted as `c16d3e7`. C4a (identity roster persistence) is **deferred** — queries built from pydantic schema descriptions only, no roster aliases in v1. C4b (phase-gated dispatch) already shipped as C1.6 (`84f6b12`+`9bc6cad`+`5c9572b`) and stays. C4d (scoped DoclingDocument) is **rolled into VR Phase C.4** as part of worker wiring. VR uses: existing `embed_texts()` for embedding (bge-m3), an ephemeral `ExtractionChunk` ArcadeDB vertex type filtered by `pipeline_run_id`, ArcadeDB's HNSW vector search via an extended `graph_store.vector_search(filters=...)`, the existing `app.services.reranker.rerank()` (bge-reranker-v2-m3, already deployed and loaded by `/v1/retrieval`), and a NEW dedicated endpoint `/v1/extraction/chunk-scope` on the `api` container that the worker HTTP-calls per dispatch. Cleanup hooks into `_terminalize_doc_and_run` + janitor task as defense-in-depth. Quality target unchanged: 10 quality gates from [[extraction-baseline-bdde417]] must hold; field_group `batch_count` ≥30% reduction on SA-2 is the headline win. See **VR section below** for full architecture, 8 phases (C.0-C.8), and acceptance.
- **2026-05-21 (rev 4 — C2 retired after user prior-experiment recall):** User remembered prior experimentation results that had already established `DOCLING_GRAPH_CHUNK_MAX_TOKENS=512`, `DOCLING_GRAPH_LLM_BATCH_TOKEN_SIZE=512`, `DOCLING_GRAPH_SYSTEM_LINKS_CHUNK_MAX_TOKENS=4096`, `DOCLING_GRAPH_SYSTEM_LINKS_LLM_BATCH_TOKEN_SIZE=4096` as the **best extraction quality** settings for this codebase + dataset. The C2 hypothesis ("doubling identity-pass `llm_batch_token_size` to 2048 will halve LLM calls without quality regression") contradicts that data — env defaults are already at the optimum. C2's *Iter 1* manifest annotation was reverted in commit `<TBD>`; the underlying `ExecutionProfile` schema + worker plumbing + service `with_runtime_defaults` wiring + the latent `max_tokens` bug fix all REMAIN in place (they're infrastructure available for any future per-pass knob experiment that targets a setting the prior experimentation didn't cover). The C2 checkpoint goal as written ("raise batch size on identity/numeric passes for wall-time win") is **retired** — env defaults are optimal; no `execution:` annotation should be added to these bundles. Future hypothetical C2-shaped checkpoints would target a different per-pass knob (e.g. per-pass `temperature` for a specific pass that benefits from lower temperature, or per-pass `max_tokens` for tighter output constraints) and should be motivated by new data, not the original wall-time hypothesis.
- **2026-05-21 (rev 3 — after second review):** Integrated 9 findings from a second code-review pass on rev 2.
  - **C4-prereq (new section)**: First-class pass phase model. `_try_advance_phase` currently treats every pass with empty `depends_on` as an "entity pass" and dispatches `system_links` once those resolve (`pipeline.py:6995`). C4b's identity-first ordering would break this. Adding an explicit `phase: identity|field_group|relationship` per manifest pass + Branch-1/2/3 rewrites that respect phases.
  - **C3 wiring**: Router enforcement + terminal-skip write moves to `_claim_and_dispatch_pass` (the single dispatch chokepoint) instead of just `derive_ontology_graph`, so Branch 1's "next entity pass" dispatches also go through the router.
  - **C1.5 post-queue tests**: Added an explicit end-to-end test (one pass queued, dispatch then fails, queued pass finishes, merge runs, summary row ends COMPLETE) before accepting the Pre-Only option.
  - **C4a roster shape**: Use ontology `identity_values: dict[str, Any]` + `display_label` + aliases (not just `system_name`) so the roster round-trips through `_extend_upstream_refs` semantics for multi-field identities.
  - **C4d ancestor/ref validation**: New explicit task to validate parent-ref resolution post-scope — preserving `texts[]`/`tables[]` arrays isn't sufficient if a retained item's `parent` points at a group/picture that's no longer reachable from `body.children`.
  - **C5 target split**: Layer A target (cross-pass, scope-independent): `sanitize_ms + table_overlay_ms < 5ms` on passes 2-N. Layer B target (same-pass retries with same scope): adds `table_normalization_ms < 5ms`. Old combined target was contradictory by construction.
  - **C5 cache ordering**: Layer A caches the FULL sanitized base doc; chunk-scope is applied AFTER the Layer A hit, never before. Layer A key stays `(document_id, content_hash)`; no `chunk_scope_hash` leak into it.
  - **C6 telemetry path**: `rehydration_ms` lands via the existing C0 `worker_diagnostics` merge (PassAttemptOutcome → _build_pass_diagnostics_dict), NOT via the service response. Task 6.2 makes that explicit.
  - **C6 substring-scan deferred**: Replacing substring-derived structural edges with `field_provenance.evidence_id` can change graph connectivity. Removed from C6's scope; deferred to a separate later checkpoint (or future plan) with explicit retrieval/structure-link quality gates.
- **2026-05-21 (rev 2 — after review):** Integrated 8 findings from code review of rev 1.
  - C1.5: dual semantic (pre-queue vs post-queue) instead of unconditional pipeline_run=FAILED, because `is_run_cancelled` treats FAILED as cancelled and would kill queued pass tasks.
  - C2: added Task 2.8b to verify the OllamaChatClient override mechanism (the cached client may ignore manifest overrides if max_tokens / chunk_max_tokens aren't threaded the same way temperature is today).
  - C3: SKIP path now writes a terminal `pipeline_pass_outputs` row (not just a stage_run) so phase advancement (`count_terminal_passes`) sees it. Added shadow / enforce mode for safe rollout.
  - C4: refactored into 4 sub-checkpoints (C4a roster persistence, C4b identity-first phase scheduling, C4c chunk-scope selection, C4d service-side ref-preserving scoped doc). In-memory roster replaced with persisted `identity_roster_json` because `derive_ontology_graph` only dispatches; per-pass workers run separately. Scoped doc preserves arrays + rewrites only `body.children` because docling refs are index-based.
  - C5: cache split into Layer A (global per-doc) + Layer B (per-pass) because table normalization is `pass_name`-keyed and would cross-pollute with a single key.
  - C6: catalog source changed — `pass_result.upstream_refs` is only populated for `document_plus_entity_refs` passes; entity-pass catalogs must come from a `template_instance` walker (shared with C4a). Wall-time gate changed from "50% of total system_links wall" to "50% of `rehydration_ms`" because the LLM-extraction share of system_links dominates the rehydration share.

---

## Status at plan creation (2026-05-21)

Shipped on `walltime/c0-telemetry`:
- **C0** — Phase 0 telemetry (`3e01a0b`): 13-metric per-pass diagnostics in `pipeline_pass_outputs.diagnostics_json`. 102 / 0 unit tests + 33 / 0 acceptance harness.
- **C1 — Phase 1** — concurrency-budget warn-only (`98daaa3`): startup line in docling-graph lifespan. The line is firing in production and immediately surfaced `effective_in_flight=16` vs `LLM_MAX_IN_FLIGHT=8` — workers blocking on the semaphore is part of the wall-time story.
- **C1 — Phase 6** — success-path payload trim (`e823abd`): `library_log` dropped from `extract_pass_response_json` + `diagnostics_json` on COMPLETE rows.

**Verified end-to-end in production:** C0 telemetry populating on all 8 Dvina passes from the in-flight regression run; C1 warning visible in docling-graph logs; P6 trim visible (radar_power_rf row that escaped via stale-fork race is the exception — see [[feedback-confirm-before-destructive-actions]] for the cancel-endpoint incident notes).

**Regression test infrastructure:**
- New baseline docs: SA-2 `ddaa9e36-2854-47c3-bc94-ff38d531dafd`, Dvina `9c8e09c7-e39f-4359-92c0-46330158c73c` (old UUIDs destroyed by accidental `/cancel`; see [[feedback-cancel-endpoint-hard-deletes]]).
- New bundle `air_defense_v3_baseline_subset` (5-pass match of bdde417 baseline) in `ontology_bundles/`. Both worktree AND main repo have a copy.
- `SA-2_Sources` (`de4751b7-6626-43d8-91ed-cb2456c96962`) default_ontology_bundle_key = `air_defense_v3_baseline_subset`. Future regression uploads to this source auto-use the subset bundle ([[regression-test-source]]).

---

## Quality gates — these MUST hold across every checkpoint

Source: [[extraction-baseline-bdde417]]. A failure on any of these = regression, not tradeoff, unless explicitly accepted by the user.

1. **Acceptance harness 33/0** (`tests/integration/test_sa2_dvina_extraction_acceptance.py`).
2. **Five structural zeros**: `derived_relationships` absent, auxiliary-equipment leaks 0, OCR-damaged names 0, self-loops 0, cross-type VARIANT_OF 0.
3. **SA-2 per-pass entity counts** ≥ baseline: `radar_identity` 23, `radar_power_rf` 34, `missile_identity` 44, `missile_kinematics` 44, `system_links` 29 rels.
4. **SA-2 system_links composition** ≥ baseline by rel_type: VARIANT_OF 15, ASSOCIATED_WITH 11, CUES 3.
5. **SA-2 `radar_power_rf.emitter_function`** fills ≥ 21/34 (Step 2 propagation floor).
6. **SA-2 `missile_kinematics.min_altitude_km`** ≥ 9/44 (v9-unblock floor).
7. **SA-2 `radar_identity` role distribution preserved**: 4 FC / 6 SEARCH / 3 HF / 10 null.
8. **SA-2 `missile_identity.designation_alias_expansion`** hits ≥ baseline (9 fires).
9. **Dvina `missile_identity` headline entity** — all 10 fields populated.
10. **Dvina `missile_kinematics`** 100% saturation (14/14) on both rows.

### Dvina-specific gate enumeration (rev 9 L9)

The "5 Dvina quality gates" referenced throughout VR (e.g. C.8 acceptance) are enumerated here for implementer clarity. Source: bdde417 baseline:

1. **`radar_identity`** — 1 RADAR_SYSTEM entity (SNR-75)
2. **`radar_power_rf`** — 1 RADAR_SYSTEM entity with ≥1 RF field populated (matches gate #2 / #3 partial on Dvina scale)
3. **`missile_identity`** — 1 MISSILE_SYSTEM entity (SA-2 family) — see gate #9 above
4. **`missile_kinematics`** — 2 MISSILE_SYSTEM entities (variants) with kinematics fields — see gate #10 above
5. **`system_links`** — 1 ASSOCIATED_WITH relationship (radar↔missile)

---

## Regression test cadence — MANDATORY at the end of EVERY checkpoint

**Rev 9 M8 clarifies VR-specific cadence:**
- **VR phases C.1–C.5 (TDD work)**: unit/integration tests + 33-test acceptance harness; no live LLM regression required for each incremental commit.
- **VR C.6 (shadow) and C.7 (Dvina narrow_only)**: Dvina-only regression. May commit **calibration-only changes** (manifest threshold tuning) after the smoke passes + router diagnostics are reviewed. **NOT promotable to main.**
- **VR C.8 (two-doc narrow_only Dvina + SA-2)**: Full regression — required for any main promotion. All 5 Dvina + 10 SA-2 quality gates must hold; SA-2 field-group `batch_count` ≥30% lower than baseline.
- **Non-VR checkpoints (C0/C1/P6/C1.5/C1.6 shipped; C5/C6 deferred)**: original full-regression rule below applies.

**Every checkpoint below ends with a full regression run.** No exceptions, including error-path-only fixes like C1.5 — even a fix that doesn't touch extraction logic must prove it doesn't regress live extraction. The 2-doc subset run is the single source of truth for both extraction performance (10 quality gates) and wall-time delta. A checkpoint is not "done" until this run is green.

The cadence — `make sure that each step in the plan concludes with a full regression test in terms of extraction performance and walltime` — is the user's explicit requirement.

### Steps (run in order at the close of every checkpoint)

1. **Code-only verification** (unit tests + acceptance harness, ~30s):
   ```bash
   cd /home/josh/development/EIP-MMDPP/.worktrees/walltime-c0-telemetry
   python3 -m pytest tests/unit/test_pass_telemetry_c0.py \
     tests/unit/test_pass_payload_trim_p6.py \
     docker/docling-graph/tests/test_extract_pass_c0_telemetry.py \
     docker/docling-graph/tests/test_concurrency_budget_c1.py \
     tests/integration/test_sa2_dvina_extraction_acceptance.py
   # Expect: all green, 33 acceptance tests pass
   ```

2. **Rebuild docling-graph if its image changed** (most checkpoints don't change it — only C2/C4/C5):
   ```bash
   docker compose -p eip-mmdpp build docling-graph
   docker compose -p eip-mmdpp up -d docling-graph worker-graph
   ```

3. **Trigger graph_only reingest** on the existing baseline docs (no re-upload — docling output is cached in MinIO):
   ```bash
   curl -sX POST "http://localhost:8005/v1/documents/ddaa9e36-2854-47c3-bc94-ff38d531dafd/reingest" \
     -H "Content-Type: application/json" -d '{"mode":"graph_only"}'
   curl -sX POST "http://localhost:8005/v1/documents/9c8e09c7-e39f-4359-92c0-46330158c73c/reingest" \
     -H "Content-Type: application/json" -d '{"mode":"graph_only"}'
   # The source's default_ontology_bundle_key sticks the subset bundle on the new runs.
   # Capture the returned pipeline_run_ids.
   ```

4. **Run the progress monitor** to get live per-pass comparisons:
   ```bash
   python3 scripts/walltime_c0_progress.py <SA-2-run-id> <Dvina-run-id>
   ```
   Wall-clock targets — these are the wall-time gates each checkpoint must clear:
   - SA-2 total ≤ baseline (5h03m) by ≥ stated checkpoint goal (0% on C1.5, modest on C2/C3, ≥30% on C4, additional on C5/C6).
   - Dvina total ≤ baseline (17m) by ≥ stated checkpoint goal.
   - Per-pass wall comparison emitted by the progress monitor; user reviews each block as it lands.

5. **Quality gates — verify ALL 10 from [[extraction-baseline-bdde417]]:**
   - Acceptance harness 33/0 ✓ (already checked in step 1, recheck after rebuild).
   - All five structural zeros ✓ (derived_relationships absent, auxiliary-equipment leaks 0, OCR-damaged names 0, self-loops 0, cross-type VARIANT_OF 0).
   - SA-2 per-pass entity counts ≥ baseline ✓ (23 / 34 / 44 / 44 / 29 rels).
   - SA-2 system_links composition ≥ baseline by rel_type ✓ (VARIANT_OF 15, ASSOCIATED_WITH 11, CUES 3).
   - SA-2 `radar_power_rf.emitter_function` fills ≥ 21/34 ✓.
   - SA-2 `missile_kinematics.min_altitude_km` ≥ 9/44 ✓.
   - SA-2 `radar_identity` role distribution ✓ (4 FC / 6 SEARCH / 3 HF / 10 null).
   - SA-2 `missile_identity.designation_alias_expansion` hits ≥ 9 ✓.
   - Dvina `missile_identity` 10 fields populated ✓.
   - Dvina `missile_kinematics` 14/14 saturation ✓.

6. **Promote or rollback decision:**
   - All 10 quality gates hold AND wall-time delta ≤ stated goal → commit + open next checkpoint's discussion gate.
   - Any gate fails → rollback the checkpoint's commit OR fix-forward (must be discussed with user, not unilateral).

### Time accounting

Each regression run on the 5-pass subset is roughly 2–3.5h depending on Ollama load (vs bdde417's 5h03m on the full 12-pass manifest). Plan accordingly: 6 checkpoints × ~3h average = ~18h of regression-run wall time across the whole plan. Code work is interleaved with these runs.

---

## Phase-discussion-before-implementation rule

Per [[feedback-phase-discussion-before-implementation]], **every checkpoint starts with a discussion gate** (Task 1 of each checkpoint below). Do NOT begin coding any checkpoint until the user explicitly says "proceed." Use `AskUserQuestion` to surface scope decisions; never assume.

---

## C1.5 — Dispatcher robustness fix (pre-C2 hardening)

**Goal:** When `derive_ontology_graph` raises BEFORE any per-pass task is queued (e.g. `UnknownBundleError`, `ConnectionError` resolving manifest), the dispatcher must terminalize cleanly: stage_run FAILED + pipeline_run FAILED. When the exception happens AFTER one-or-more per-pass tasks have already been queued, behavior is different — see semantic decision below.

**Why:** Discovered 2026-05-21 — a stale worker-1 couldn't see the new subset bundle, the dispatcher raised `UnknownBundleError`, but the stage_run was marked RUNNING *before* the bundle resolve. The exception bubbled through the celery guard but no FAILED transition was emitted, leaving the pipeline_run wedged in PROCESSING for 8 hours. Future regression tests will hit this any time the dispatcher partially fails.

**Semantic decision (forced by review):** `is_run_cancelled()` (`app/services/run_phase_dispatch.py:438`) treats `status='FAILED'` as cancelled. Any already-queued pass task that calls `is_run_cancelled()` will bail. The plan must pick ONE of these behaviors:

  - **Option Pre-Only (Recommended):** Terminalize PipelineRun=FAILED ONLY when the exception happens BEFORE the first per-pass task was queued. Once at least one task has been queued, the dispatcher stage_run is still marked FAILED, but the pipeline_run is LEFT in PROCESSING so the queued passes run to completion. The reconciler then handles the terminal transition based on what those passes produced.
  - **Option Fail-Fast:** Always terminalize PipelineRun=FAILED on dispatcher failure, even if some passes were queued — accepting that those queued tasks will bail at their next cancel-check. Simpler but loses partial-work value.

Recommend Option Pre-Only — preserves any successful upstream work and gives the reconciler a clean view.

**Files:**
- Modify: `app/workers/pipeline.py` (~line 7772, `derive_ontology_graph` task body)
- Test: `tests/unit/test_derive_ontology_graph_dispatcher_failure.py` (new)

**Acceptance:**
- `derive_ontology_graph` task wraps its per-pass dispatch logic in try/except.
- On pre-queue exception (no passes queued yet): dispatcher stage_run FAILED + pipeline_run FAILED + error_message set.
- On post-queue exception (≥1 pass queued before the failure): dispatcher stage_run FAILED, pipeline_run stays PROCESSING, reconciler handles eventual terminal state.
- A new unit test for the pre-queue path: mock the bundle loader to raise `UnknownBundleError`, invoke `derive_ontology_graph` directly, assert (a) stage_run row written with FAILED, (b) PipelineRun status moved to FAILED, (c) no Celery `.delay()` calls were made.
- A second unit test for the post-queue path: mock to allow N pass dispatches then raise on (N+1), assert PipelineRun stays PROCESSING + stage_run FAILED with error_message naming the queued count.

**Open scope decisions (Task 1 — discussion gate):**
- (a) Pre-Only vs Fail-Fast (above). Recommend Pre-Only.
- (b) Are there OTHER stages with the same RUNNING-without-cleanup bug (e.g. `derive_text_chunks_and_embeddings`)? Recommend: scope just `derive_ontology_graph` for this fix and TODO the broader audit.
- (c) Should the fix include an UPSERT path so a re-run can reclaim a stuck-RUNNING row? Recommend: leave reclaim to the `reconcile_ontology_graph_runs` heartbeat; fix prevents *new* stuck states.

**Tasks:**
- [ ] **Task 1.1: Discussion gate** — surface the two scope decisions above via `AskUserQuestion`. Wait for user "proceed" before touching code.
- [ ] **Task 1.2: Read current `derive_ontology_graph` body** (`app/workers/pipeline.py` around line 7772) to confirm where stage_run is set RUNNING and where dispatch happens.
- [ ] **Task 1.3: Write the failing test** — `tests/unit/test_derive_ontology_graph_dispatcher_failure.py` asserting FAILED stage_run + FAILED pipeline_run on bundle-load failure. Run with `pytest tests/unit/test_derive_ontology_graph_dispatcher_failure.py -v` and confirm RED.
- [ ] **Task 1.4: Implement try/except wrap** around the dispatch block. Track `_passes_queued: int` inside the block. In the except: ALWAYS write stage_run FAILED with error_message including the queued count. Terminalize pipeline_run=FAILED ONLY when `_passes_queued == 0`. Re-raise so Celery sees the failure for retry-counting.
- [ ] **Task 1.5: Run new test + full C0/C1/P6 verification suite** — confirm GREEN + 33/0 acceptance harness still holds.
- [ ] **Task 1.6: Commit** as `fix(walltime): dispatcher marks stage_run FAILED on early-dispatch exceptions`. Reference incident in commit body.
- [ ] **Task 1.7: Post-queue ledger end-to-end test** (review finding 3). New `tests/integration/test_dispatcher_post_queue_recovery.py`: arrange a 2-pass manifest where pass 1 dispatch succeeds and pass 2 raises. Assert (a) pass 1 stage_run + pipeline_pass_outputs row both reach COMPLETE; (b) `derive_ontology_graph` summary stage_run is FAILED; (c) PipelineRun stays PROCESSING while pass 1 finishes; (d) `_try_advance_phase` proceeds past pass 1 and eventually dispatches merge; (e) the run's final summary StageRun row ends in COMPLETE (not FAILED) once merge succeeds. This locks in the Pre-Only semantic across the full ledger lifecycle.
- [ ] **Task 1.8: Full regression run** per the cadence above (steps 1–6 from "Regression test cadence"). Wall-time target: ≤ bdde417 baseline within ±2% (this fix has no wall-time effect by design; the run proves it has no extraction regression either). Quality gates: all 10 must hold.
- [ ] **Task 1.9: Report results to user**, including the per-pass wall + count comparison from the progress monitor. Wait for promote / rollback decision before opening C4-prereq.

**Estimated effort:** 30–45 min code + tests + ~2–3h regression run = ~3–4h total.

---

## C1.6 — First-class pass phase model (prerequisite for C3 + C4)

**Goal:** Replace the implicit "any pass with empty `depends_on` is an entity pass" rule (`pipeline.py:6995`) with an explicit `phase: identity | field_group | relationship` declared per manifest pass. `_try_advance_phase` Branches 1/2/3 then operate on phases, not on the emptiness of `depends_on`. This is the load-bearing prerequisite for C4b (identity-first ordering) and C3 (router fired at the right dispatch chokepoint).

**Why:** Discovered in review of rev 2 — if C4b adds `depends_on: [radar_identity, missile_identity]` to field-group passes to enforce ordering, those field-group passes drop OUT of the implicit "entity_passes" set (`[p.name for p in manifest.passes if not p.depends_on]` at line 6995). Branch 2 of `_try_advance_phase` would then dispatch `system_links` as soon as the identity passes resolve — BEFORE field-group passes complete. The whole identity-first design collapses without explicit phases.

**Files:**
- Modify: `app/services/ontology_bundles.py` (`BundleManifest`/pass def — add required `phase: Literal["identity","field_group","relationship"]` field; default = derived from current logic for back-compat read, but new bundles must declare it).
- Modify: `ontology_bundles/air_defense_v3/manifest.yaml` + `ontology_bundles/air_defense_v3_baseline_subset/manifest.yaml` — annotate every pass with `phase:`. Identity = `radar_identity`, `missile_identity`. Field-group = the other 9 entity passes (or 2 in the subset). Relationship = `system_links`.
- Modify: `app/workers/pipeline.py:_try_advance_phase` — Branches rewritten in phase order:
  - **Branch 1** (identity): dispatch next identity pass while in-flight < cap.
  - **Branch 2** (field_group): only AFTER all identity passes terminalize. Dispatch field-group passes while in-flight < cap.
  - **Branch 3** (relationship/system_links): only AFTER all field_group passes terminalize (or after identity if no field_group passes exist in the bundle).
  - **Branch 4** (merge): only AFTER relationship pass terminalizes.
- Modify: `_claim_and_dispatch_pass` (called by Branch 1/2/3) — single chokepoint where C3's router check will land later.
- Test: `tests/unit/test_pass_phase_model.py` — manifest validation: every pass has a phase; phases sort in order identity → field_group → relationship.
- Test: `tests/unit/test_try_advance_phase_by_phase.py` — Branch 1 dispatches only identity until they terminalize; Branch 2 only fires field_group AFTER all identity are terminal; Branch 3 only fires system_links after all field_group are terminal; Branch 4 fires merge only after relationship terminalizes; back-compat case (no field_group passes) skips Branch 2.

**Acceptance:**
- Every manifest pass declares `phase:` (validation error if missing in a fresh bundle).
- Old runs with no `phase` field don't crash — back-compat reader infers it (identity=name-matches-`*_identity`, relationship=`input_mode=document_plus_entity_refs`, otherwise field_group). Inferred values are logged as INFO.
- All 10 quality gates hold on the existing 5-pass subset run (which contains identity + field_group + relationship — full coverage of all three phases).

**Open scope decisions (Task 1 — discussion gate):**
- (a) Phase enum values — `identity`/`field_group`/`relationship`, or `extraction_first_pass`/`extraction_field_pass`/`linking`? Recommend the former (shorter, matches the conceptual model).
- (b) Back-compat: should the validator REQUIRE `phase:` on every pass, or INFER+LOG when missing? Recommend INFER+LOG for the first ship to avoid breaking the existing bundles outside this repo; tighten to required in a follow-up.
- (c) Per-phase concurrency caps — should each phase have its own `pass_concurrency_per_document` cap, or share the global cap? Recommend share the global cap for now.

**Tasks:**
- [ ] **Task 1.6.1: Discussion gate.**
- [ ] **Task 1.6.2: Write failing manifest-validation test** — assert a manifest without `phase:` per pass infers + logs INFO; assert a manifest WITH bad `phase:` values raises ValidationError.
- [ ] **Task 1.6.3: Extend `BundleManifest` pydantic model** with the `phase` field + back-compat inference.
- [ ] **Task 1.6.4: Annotate `phase:` on every pass** in both manifest.yaml files.
- [ ] **Task 1.6.5: Write failing `_try_advance_phase` integration test** covering all four Branches + the back-compat (no field_group) case.
- [ ] **Task 1.6.6: Rewrite `_try_advance_phase` branches** to operate on phases. Keep the existing `dispatched_phases` JSONB key shape; only the gating logic changes.
- [ ] **Task 1.6.7: Refactor `_claim_and_dispatch_pass`** to be the single dispatch chokepoint. (Sets up C3's router hook.)
- [ ] **Task 1.6.8: Run unit tests + acceptance harness** — expect GREEN + 33/0.
- [ ] **Task 1.6.9: Commit** as `feat(walltime): C1.6 — explicit pass phase model in _try_advance_phase`.
- [ ] **Task 1.6.10: Full regression run** per the cadence. Wall-time target: ≤ bdde417 baseline (this is a refactor, not a behavior change — wall is identical). Quality gates: all 10 must hold.
- [ ] **Task 1.6.11: Report results** + promote/rollback decision before opening C2.

**Estimated effort:** Code + tests ~3–4h. Regression run ~3h.

---

## C2 — Per-pass batch profiles in the manifest (Phase 2)

> ⚠️ **RETIRED — DO NOT IMPLEMENT.** See rev 4 in revision history for retirement rationale. User's prior experimentation showed `DOCLING_GRAPH_CHUNK_MAX_TOKENS=512` + `DOCLING_GRAPH_LLM_BATCH_TOKEN_SIZE=512` (+ 4096/4096 for `system_links`) are already at optimal extraction quality. The C2 hypothesis ("doubling batch size cuts LLM calls without quality regression") was invalidated. The `ExecutionProfile` infrastructure shipped in commit `4161514` and remains available for any FUTURE knob experiment that targets a setting the prior experimentation didn't cover (e.g. per-pass `temperature`), but the per-pass `llm_batch_token_size` annotation should NOT be re-introduced on identity/numeric passes. **The rest of this section is preserved as historical reference. An implementer encountering this section should treat it as documentation, not as actionable tasks.**

---

<details>
<summary><i>Historical C2 content (do not implement — click to expand)</i></summary>

**Goal:** Each manifest pass can declare its own `chunk_max_tokens`, `llm_batch_token_size`, `temperature`, `max_tokens`. Worker plumbs them through `_build_extract_pass_request`; service applies them in `build_pipeline_config`. Initial profiles target 2× batch-count reduction on identity/numeric passes without quality regression.

**Why:** From the user's original handoff plan. The default is `chunk_max_tokens=512` / `llm_batch_token_size=1024` for all passes except `system_links` (4096). C0 telemetry showed `batch_count=6` per Dvina pass — raising batch size should cut LLM calls 2–4×. Risk is per-pass: larger batches dilute attention and can erode precision on numeric passes.

**Files:**
- Modify: `app/services/ontology_bundles.py` (the `BundleManifest` pydantic model — add optional `execution: ExecutionProfile` field on each pass def)
- Modify: `ontology_bundles/air_defense_v3/manifest.yaml` AND `ontology_bundles/air_defense_v3_baseline_subset/manifest.yaml` (add per-pass `execution:` blocks with the chosen profiles)
- Modify: `app/workers/pipeline.py:_build_extract_pass_request` (forward `execution` fields onto the `/extract-pass` request body; current code already plumbs `temperature` + `llm_batch_token_size` per-pass — extend to include `chunk_max_tokens` + `max_tokens`)
- Modify: `docker/docling-graph/app/schemas.py` (`ExtractPassRequest` — add fields)
- Modify: `docker/docling-graph/app/config_builder.py:build_pipeline_config` (accept per-pass overrides)
- Test: `tests/unit/test_manifest_execution_profile.py` (new — schema validation)
- Test: `tests/unit/test_extract_pass_request_threads_execution.py` (new — request building threads overrides)
- Test: `docker/docling-graph/tests/test_config_builder_per_pass_overrides.py` (new — config_builder applies overrides)

**Acceptance:**
- Manifest schema accepts optional `execution:` block per pass; defaults are inherited from existing `DOCLING_GRAPH_*` env vars when omitted.
- Profile applied per-pass: identity 2048 / numeric 2048 (try 4096 on a second pass) / system_links keeps 4096.
- A/B run vs `air_defense_v3_baseline_subset` shows: SA-2 per-pass `batch_count` reduced ≥ 30% on at least one pass (typically `missile_identity`, `missile_kinematics`); ALL 10 quality gates hold; per-pass entity/fill counts within ±5% of baseline.

**Open scope decisions (Task 1 — discussion gate):**
- (a) Which passes get bumped first? Recommend: start identity-only (radar_identity + missile_identity at 2048). Identity passes have the strongest "more context = better resolution" intuition.
- (b) Add a manifest-level default for profile values, or always inherit from env? Recommend: env stays the default, manifest overrides.
- (c) A/B method — same-doc back-to-back (network warm) vs separate fresh runs? Recommend: back-to-back graph_only reingests on the same doc, ~30 min apart, to compare under similar Ollama load.

**Tasks:**
- [ ] **Task 2.1: Discussion gate.**
- [ ] **Task 2.2: Map `ExtractPassRequest` schema** (`docker/docling-graph/app/schemas.py`) to confirm current request shape and find where to add fields.
- [ ] **Task 2.3: Write failing manifest-schema test** — `tests/unit/test_manifest_execution_profile.py` asserts a pass with `execution: {chunk_max_tokens: 512, llm_batch_token_size: 2048, temperature: 0.1, max_tokens: 8192}` loads cleanly, AND a pass without it loads with defaults=None.
- [ ] **Task 2.4: Extend `BundleManifest`** schema in `app/services/ontology_bundles.py` with the optional `ExecutionProfile` pydantic model; run the failing test → GREEN.
- [ ] **Task 2.5: Write failing request-builder test** — assert `_build_extract_pass_request` propagates all 4 execution fields when present in pass_def, omits them when None.
- [ ] **Task 2.6: Extend `_build_extract_pass_request`** to thread the new fields onto the HTTP body.
- [ ] **Task 2.7: Write failing service-side test** — `docker/docling-graph/tests/test_config_builder_per_pass_overrides.py` asserts `build_pipeline_config` uses per-pass overrides when present, falls back to env when not.
- [ ] **Task 2.8: Extend `build_pipeline_config`** to accept the new override kwargs; update `extract_pass` endpoint to pass them through.
- [ ] **Task 2.8b: Verify the OllamaChatClient override path.** `OllamaChatClient` is constructed once per process (`ollama_clients.py:79` uses `max_tokens=settings.docling_graph_llm_max_tokens` as a constructor arg) and cached by `get_docling_graph_client()`. Per-pass `temperature` and `llm_batch_token_size` already bypass the cached constructor — trace exactly how (likely via per-call override on the `chat()` method or a build-time `LlmCallOverrides` object). Reuse the same mechanism for `max_tokens` and `chunk_max_tokens`. Add a focused unit test asserting that a manifest `max_tokens=8192` override actually reaches the LLM call site, not just the request body. Surface the mechanism in the commit message so the next checkpoint's reviewer can verify the chain.
- [ ] **Task 2.9: Add `execution:` blocks** to BOTH `ontology_bundles/air_defense_v3/manifest.yaml` and `ontology_bundles/air_defense_v3_baseline_subset/manifest.yaml`. Start with identity passes only (radar_identity=2048, missile_identity=2048). Other passes left default for first A/B.
- [ ] **Task 2.10: Run full unit-test suite + acceptance harness** — expect GREEN + 33/0.
- [ ] **Task 2.11: Rebuild docling-graph + restart worker-graph.**
- [ ] **Task 2.12: Trigger graph_only reingest** for both docs; run the progress monitor.
- [ ] **Task 2.13: Compare per-pass `batch_count` + entity/fill counts** vs baseline. Promote OR roll back per the quality gates.
- [ ] **Task 2.14: Iteration 2** — if identity-only A/B was clean, bump numeric passes (`missile_kinematics`, `radar_power_rf`) to 2048. Re-run.
- [ ] **Task 2.15: Iteration 3** — if iteration 2 was clean, try 4096 on numeric passes only. Re-run.
- [ ] **Task 2.16: Commit** as `feat(walltime): C2 — per-pass batch profiles via manifest`. Reference the A/B results in the commit body.
- [ ] **Task 2.17: Full regression run** per the cadence (steps 1–6). Quality gates: all 10 must hold. Wall-time target: SA-2 ≥ 15% reduction (batch-count drop of ≥ 30% on at least one pass should translate to ~15-20% wall savings under the current Ollama contention).
- [ ] **Task 2.18: Report results** + promote/rollback decision before opening C3.

</details>

---

## VR — Vector Router (supersedes C3 + C4)

> **Status:** rev-9 (2026-05-21) architecture replacing lexical C3 + C4 with vector retrieval + cross-encoder rerank. Lexical C3 (`db1ea53`) reverted as `c16d3e7`. Below is the authoritative plan; legacy C3/C4 sections below VR are SUPERSEDED.
>
> **Selected-scope handoff** (rev 9 H1): `_claim_and_dispatch_pass` passes the **small `chunk_scope` JSON dict** (mode + self_refs, kilobytes) as a Celery kwarg to `derive_ontology_graph_pass.delay(...)`. The per-pass task body loads the full doc from MinIO (as today), then calls **`apply_chunk_scope(doc_json, chunk_scope)` inside the task body** before `_execute_pass_attempt`. **Scoped doc JSON is NEVER passed through Celery** (potentially megabytes).
>
> **Endpoint output modes**: `selected_refs` (router selected K refs, worker narrows) / `full` (no narrowing — fallback or identity/required/relationship pass) / `would_skip` (router believes no relevant chunks; **worker decides per `VECTOR_ROUTER_MODE`**). Endpoint never decides "skip" unilaterally.
>
> **Worker dispatch modes**: `disabled` (router not invoked) / `shadow` (router runs, diagnostics persisted, always RUN_FULL) / **`narrow_only`** (ship target — narrows on `selected_refs`; fails open to RUN_FULL on `would_skip` with `fail_open_reason='would_skip_in_narrow_only_mode'`) / `skip_enforce` (DEFERRED indefinitely). Default on first deploy = `shadow`. **NEVER narrowed:** identity passes, required passes, AND **relationship passes (`system_links`)** — narrowing relationship-pass input risks composition-gate violations.
>
> **Critical-evidence-only recall gate** (rev 9 H2): the C.2b deterministic test asserts `set(selected_refs_pre_rerank) ⊇ set(critical_evidence_refs)` where critical evidence = (a) 100% of Dvina baseline evidence refs (all 5 passes), and (b) only SA-2 evidence refs for entities counted by gates #5 (`emitter_function` 21/34) and #6 (`min_altitude_km` 9/44). Other SA-2 provenance is informational, not blocking. With `top_k=20`, this is achievable; "all baseline provenance" would be impossible.
>
> **Promotability gate** (per M11 in rev 7): C.6 and C.7 are Dvina-only **CALIBRATION/SMOKE** runs — NOT promotable. Only C.8 (two-doc Dvina + SA-2 `narrow_only`) is promotable. All 10 SA-2 + 5 Dvina quality gates must hold + SA-2 field-group `batch_count` ≥30% lower than baseline before main-merge.
>
> **Load-bearing gate before any live LLM iteration** (rev 10 canonical wording — supersedes the rev 7 H4 phrasing earlier in the revision history): the **C.2b deterministic test** asserts `set(pre_rerank_top_n_candidates) ⊇ set(normalized_critical_evidence_refs)` where critical evidence = (a) 100% Dvina baseline evidence across all 5 passes, AND (b) ONLY SA-2 baseline evidence for entities counted by quality gates #5 (`emitter_function` 21/34) and #6 (`min_altitude_km` 9/44). With `top_k=20` this is achievable; "all baseline provenance" would be impossible by construction. Pre-rerank validation; rerank-stage critical recall is a separate C.6 acceptance check (rev 10 M5).

**Goal:** For each field-group pass, deterministically decide whether to RUN (with a narrowed chunk scope) or SKIP based on vector-similarity + cross-encoder relevance of the document's chunks to the pass's pydantic-schema-derived query.

**Why:** Cue-list lexical matching (the original C3 + C4c design) is insufficient for the corpus — schema-derived natural-language queries embedded via bge-m3 capture relevance more reliably than keyword substring matching, especially for the variable vocabulary in equipment-spec documents.

**Architecture summary:**

```
Worker (dispatcher: derive_ontology_graph)
  ├── manifest load
  ├── build_extraction_index(doc, run_id, doc_id)
  │     ↳ embed_texts(docling_elements, query=False)
  │     ↳ Bulk INSERT ExtractionChunk vertices (idempotent: delete-then-insert)
  └── per-pass dispatch via _claim_and_dispatch_pass
       ↳ For field_group passes: HTTP POST /v1/extraction/chunk-scope
       ↳ For identity / required passes: short-circuit (always RUN_FULL)

API container (new endpoint /v1/extraction/chunk-scope)
  ├── embed_texts([query], query=True)                ← bge-m3 query mode (via run_in_executor)
  ├── search_extraction_chunks(store, query_emb,      ← rev 11: over-fetch + Python post-filter
  │       pipeline_run_id=run_id,                       ArcadeDB HNSW is post-filter only (P1 gap);
  │       desired_top_n=profile.top_n_candidates,       bare vector_search(filters=...) causes starvation.
  │       score_threshold=min_similarity)                Overfetch top_k=max(desired_top_n*10,500),
  │                                                      retry once at 2000 if needed.
  │                                                      Returns (results, ChunkSearchDiagnostics).
  ├── if empty AND fallback_to_full → return mode=full       (worker dispatches RUN_FULL)
  ├── if empty AND NOT fallback_to_full → return mode=would_skip  (worker decides per VECTOR_ROUTER_MODE)
  ├── reranker.rerank(query, candidates)              ← bge-reranker-v2-m3
  │     ↳ on rerank EXCEPTION → return mode=full with diagnostic.reason='reranker_unavailable'
  └── return mode=selected_refs, top-K self_refs + router_diagnostics (includes ChunkSearchDiagnostics)

Worker (per-pass dispatch in _claim_and_dispatch_pass)
  ├── if pass.phase in (identity, relationship) OR pass.required → SHORT-CIRCUIT (no router call, full doc)
  ├── HTTP POST /v1/extraction/chunk-scope (httpx.Client, sync, timeout=10s)
  │     ↳ on HTTP error (timeout, 5xx, conn refused) → router_response.mode=full,
  │                                                     diagnostics.fallback_reason='http_<error>'
  ├── DECIDE effective_chunk_scope per VECTOR_ROUTER_MODE (rev 10 H2):
  │     'disabled'    → effective_chunk_scope=None
  │     'shadow'      → effective_chunk_scope=None (ALWAYS — never narrow);
  │                     diagnostics.shadow_skipped_narrowing = (router_response.mode == 'selected_refs')
  │     'narrow_only' → mode=selected_refs → effective_chunk_scope = router_response.chunk_scope
  │                   → mode=full          → effective_chunk_scope = None (run full as router decided)
  │                   → mode=would_skip    → effective_chunk_scope = None + diagnostics.fail_open_reason
  │                                          = 'would_skip_in_narrow_only_mode' (fail open)
  ├── Celery dispatch: derive_ontology_graph_pass.delay(
  │       document_id, run_id, pass_name,
  │       chunk_scope=effective_chunk_scope,         ← what worker actually applies (or None)
  │       router_diagnostics=router_diagnostics,      ← full raw router decision for telemetry
  │   )
  └── selected_token_estimate / full_doc_token_estimate > 0.80 → WARNING (narrowing ineffective)

Per-pass task (derive_ontology_graph_pass body)
  ├── load full doc_json from MinIO (unchanged from today)
  ├── if chunk_scope and chunk_scope.get('mode') == 'selected_refs':
  │     doc_json = apply_chunk_scope(doc_json, chunk_scope)   ← scoped doc built INSIDE the task
  └── _execute_pass_attempt(..., doc_json=doc_json)
       Backward-compat: missing chunk_scope kwarg defaults to None → full doc (rev 10 M7 test).

Worker (terminalization: _terminalize_doc_and_run)
  └── cleanup_extraction_index(run_id) — best-effort DELETE

Worker (per-pass terminal save: _save_terminal_pass_output)
  └── merge router_diagnostics into pipeline_pass_outputs.diagnostics_json.router

Janitor task (scheduled, hourly) — see rev 9 H3 worker algorithm:
  1. ArcadeDB: query distinct pipeline_run_ids with created_at < NOW - 24h
  2. Postgres: batched fetch of pipeline_runs.status for those IDs
  3. Compute purge set: terminal (COMPLETE/FAILED/PARTIAL_COMPLETE) OR orphan (no Postgres row)
  4. ArcadeDB: bulk DELETE WHERE pipeline_run_id IN (purge_set)
  NEVER expressed as a single cross-store SQL — ArcadeDB cannot read Postgres state.
```

**Key reuses** (no new model infrastructure):
- `embed_texts()` for chunk + query embeddings (existing bge-m3 endpoint)
- `app.services.reranker.rerank()` for cross-encoder (existing bge-reranker-v2-m3, already loaded in api container)
- `graph_store.vector_search()` with a small `filters: dict | None` extension
- Existing ArcadeDB HNSW indexes (auto-created on the `embedding` property)

**Locked decisions** (from rev-5 discussion gates):
- **Q1**: revert lexical C3 (done as `c16d3e7`)
- **Q2**: schema audit done — descriptions already RICH (91% RICH, 9% OK, 0% SPARSE/EMPTY); no improvements needed
- **Q3**: new dedicated endpoint `/v1/extraction/chunk-scope` on api container (option Q3.b — keeps reranker model in api process, zero blast radius on RAG retrieval)
- **Q4**: schema descriptions only — no roster aliases, **C4a deferred** (saves ~3-4 days; promote per-pass if quality fails on equipment-recall)
- **Q5**: ArcadeDB ephemeral table (option β-persisted) — vertices filtered by `pipeline_run_id`, dropped at terminal
- **Q5a**: build index inside `derive_ontology_graph` dispatcher
- **Q5b**: extend `graph_store.vector_search()` with `filters` parameter (translates to ArcadeDB WHERE clause before HNSW lookup)
- **Q5c**: cleanup hook in `_terminalize_doc_and_run` + scheduled janitor task as defense-in-depth
- **Q6**: Dvina first (cheap iteration loop), SA-2 only after Dvina shadow + enforce both pass

**ExtractionChunk vertex schema:**

```python
"ExtractionChunk": [
    ("vertex_id", "STRING"),         # PK: f"{pipeline_run_id}:{self_ref}"
    ("pipeline_run_id", "STRING"),   # filter dimension
    ("document_id", "STRING"),
    ("self_ref", "STRING"),          # e.g. "#/texts/12"
    ("chunk_text", "STRING"),
    ("embedding", "ARRAY_OF_FLOATS"),  # dim=1024 (bge-m3); matches existing TextChunk vector index dim
    ("page_number", "INTEGER"),
    ("modality", "STRING"),           # text | table | picture_caption
    ("created_at", "TIMESTAMP"),      # default NOW(); load-bearing for janitor orphan cleanup
]
# HNSW vector index on `embedding` (cosine, dim=1024)
# B-tree index on `pipeline_run_id` for filter performance
# B-tree index on `created_at` for janitor age-based cleanup
#
# Janitor (hourly) — implemented as a worker algorithm, NOT a single SQL statement
# (ArcadeDB cannot read Postgres state). See rev 9 H3 worker algorithm:
#   1. ArcadeDB: SELECT DISTINCT pipeline_run_id WHERE created_at < NOW - 24h
#   2. Postgres: batched lookup of pipeline_runs.status for those IDs
#   3. Compute purge_set = {IDs with status in COMPLETE/FAILED/PARTIAL_COMPLETE} ∪ {orphan IDs}
#   4. ArcadeDB: DELETE WHERE pipeline_run_id IN (purge_set)
# Catches: (a) terminal-run leftovers where _terminalize_doc_and_run cleanup failed;
#          (b) orphan rows whose pipeline_run record was deleted/never created.
```

**Per-pass manifest config (C.2b ships conservative defaults across both manifests):**

```yaml
- name: radar_power_rf
  phase: field_group
  retrieval:
    min_similarity: 0.45         # cosine threshold
    top_n_candidates: 50         # pre-rerank
    top_k: 20                    # post-rerank
    fallback_to_full: true       # conservative default: empty match → mode=full (worker dispatches RUN_FULL).
                                 # Set to false ONLY after C.6 shadow data shows the pass reliably routes
                                 # would_skip on off-topic doc samples without false-positives.
```

**Identity, required, and relationship passes have NO `retrieval:` block** — they are bypassed by the router short-circuit in `_claim_and_dispatch_pass`.

**Top-level acceptance for VR:**
- All 5 Dvina quality gates hold (no field-extraction regression)
- All 10 SA-2 quality gates hold (#5 emitter_function 21/34 and #6 min_altitude_km 9/44 most at risk)
- SA-2 field-group `batch_count` ≥ 30% lower than bdde417 baseline
- Acceptance harness 33/0
- Dvina + SA-2 wall: target same or lower than bdde417

**Eight phases (C.0 → C.8):**

| Phase | Description | Deliverable |
|---|---|---|
| **C.0** | Dvina baseline regression on `c16d3e7` (post-revert, pre-vector-router) | Quality confirmed at known-good baseline before vector work begins. NOT promotable; smoke check only. |
| **C.1** | `ExtractionChunk` vertex type + `graph_store.vector_search(filters=…)` extension + filter-starvation adversarial test + **rev 11: `search_extraction_chunks()` over-fetch helper** | Schema + retrieval primitive. **Rev 11 finding**: ArcadeDB HNSW is post-filter (P1 gap); filtered `vector_search` causes starvation. Workaround: over-fetch unfiltered + Python post-filter via `app/services/extraction_chunk_search.py`. Filter-starvation test stays as acceptance gate. |
| **C.2** | `extraction_chunk_index.py` (build + cleanup helpers) + chunk rendering rules for text/table/picture | Module reusable from dispatcher + terminalize hook + janitor |
| **C.2b** ★NEW★ | `RetrievalProfile` pydantic + `build_retrieval_query(pass_def)` + offline-recall fixture test against bdde417 evidence refs | **Load-bearing gate.** Catches "router loses required evidence" before any live LLM iteration. |
| **C.3** | New endpoint `/v1/extraction/chunk-scope` on api container | HTTP-callable retrieve+rerank pipeline. Reranker failure → fail open to RUN_FULL (NOT vector-only). |
| **C.4** | Worker wiring: dispatcher MinIO load + index build + HTTP call + scoped-doc builder (`apply_chunk_scope`) + `_terminalize_doc_and_run` cleanup + hourly janitor (24h + orphan-rows policy) | Full dispatch path uses vector router with `narrow_only` semantics. HTTP/build/reranker errors fail open. SKIP determinations are diagnostic-only. |
| **C.5** | Per-pass `retrieval:` blocks in both manifests (calibrated thresholds) | Manifest filled in once C.6 shadow data is in |
| **C.6** | Dvina **shadow-mode** regression (router runs, persists decisions in `pipeline_pass_outputs.diagnostics_json.router`, always dispatches RUN_FULL) | **CALIBRATION/SMOKE — NOT promotable.** Confirm router decisions match intuition; tune thresholds. **Rev 10 M5 — post-rerank critical evidence gate:** the persisted `selected_refs` (post-rerank, NOT pre-rerank candidates) must cover the same normalized critical evidence refs the C.2b deterministic gate asserted. A reranker that drops a critical chunk FAILS C.6 before proceeding to C.7. **Rev 10 M6 — would_skip_if_fallback_disabled:** any pass on Dvina (relevant doc) flagged with `would_skip_if_fallback_disabled=true` is a recalibration trigger (lower `min_similarity`) before C.7. |
| **C.7** | Dvina **`narrow_only`-mode** regression | **CALIBRATION/SMOKE — NOT promotable.** Quality gates hold; batch_count drops; would-skip decisions logged but fail open |
| **C.8** | Two-doc `narrow_only` regression (Dvina + SA-2) — **the promotable gate** | **Rev 12 three-layer acceptance:** (L1 PROMOTABLE) all 10 SA-2 + 5 Dvina quality gates hold + SA-2 field-group `batch_count` ≥30% lower than **C.0 post-C1.6** (NOT bdde417) + field-group long-pole wall materially shorter than C.0's. (L2 REVIEW) Total wall recovers most of the C1.6 serialization tax vs `bdde417`. (L3 FUTURE) ≥50% vs `bdde417` is the program target — VR alone unlikely to clear; tracked for post-VR C5/C6. **C1.6 justification check:** if batch_count drops < 30% OR field-group wall barely budges, C1.6's cost is NOT recouped — surface a C1.6 rollback discussion before main-promotion. Promote to main only if Layer 1 passes AND C1.6 justification holds. |
| ~~C.9~~ | ~~SA-2 `skip_enforce` on optional passes~~ — **DEFERRED indefinitely** | SKIP enforcement requires off-topic-doc corpus + false-positive gates + UI semantics; separate future plan if production evidence warrants. |

**Risk + fallback hierarchy:**
- If C.6 shadow logs SKIP on Dvina (relevant doc) → lower min_similarity, recalibrate
- If C.7 quality gates fail on Dvina → top_k too aggressive or reranker miscalibrated
- If C.8 SA-2 quality fails on a specific pass (e.g. gate #6) → promote THAT pass to include Tier-1 roster aliases (requires C4a — one pass, not all)
- If C.8 SA-2 batch_count doesn't drop ≥30% → top_k too generous; tighten per-pass
- If C.8 SA-2 fails on multiple gates → consider hybrid (lexical alias filter + vector rerank)

**Files:**

| Path | Status |
|---|---|
| `app/services/extraction_chunk_index.py` | NEW (C.2) |
| `app/services/extraction_chunk_search.py` | NEW (**C.1** — rev 11 promoted from C.3) — `search_extraction_chunks()` over-fetch + Python post-filter; the required path around ArcadeDB's post-HNSW filter behavior. C.3 endpoint calls this; never calls `vector_search(filters=...)` directly for VR vector queries. |
| `app/api/v1/extraction_routing.py` | NEW (C.3) — FastAPI router |
| `app/schemas/extraction_routing.py` | NEW (C.3) — request/response models |
| `app/services/arcadedb_schema.py` | MODIFIED (C.1) — add `ExtractionChunk` |
| `app/services/graph_store.py` | MODIFIED (C.1) — `filters` param on `vector_search` |
| `app/services/scoped_docling_document.py` | **NEW (C.4)** — `apply_chunk_scope(doc_json: dict, chunk_scope: dict) -> dict`. Preserves `texts[]`/`tables[]`/`pictures[]` array positions; rewrites only `body.children` traversal; validates parent reachability post-scope. (rev 10 M7) |
| `app/workers/pipeline.py` | MODIFIED (C.4) — dispatcher build, dispatch-time HTTP call to `/v1/extraction/chunk-scope`, **`effective_chunk_scope` decision per rev 10 H2** (shadow always None; narrow_only narrows on `selected_refs` else None with fail_open_reason), terminalize cleanup, janitor task algorithm (cross-store; see rev 9 H3) |
| `app/services/ontology_bundles.py` | MODIFIED (**C.2b** per rev 9 H4) — `RetrievalProfile` pydantic + `PassManifest.retrieval` field |
| `app/services/extraction_query_builder.py` | NEW (C.2b) — `build_retrieval_query(pass_def, template_cls) -> str` |
| `ontology_bundles/air_defense_v3/manifest.yaml` | MODIFIED (**C.2b** per rev 9 H4) — `retrieval:` per field_group pass with **conservative defaults** (`fallback_to_full: true`, `min_similarity: 0.45`, `top_n_candidates: 50`, `top_k: 20`). C.5 may later tune these values based on C.6 shadow data. |
| `ontology_bundles/air_defense_v3_baseline_subset/manifest.yaml` | MODIFIED (**C.2b** per rev 9 H4) — same conservative defaults |
| `tests/integration/test_vector_router_offline_recall_deterministic.py` | NEW (C.2b) — **load-bearing hermetic gate**. Validates vector recall PRE-RERANK against critical evidence refs (100% Dvina + SA-2 gates #5/#6 evidence only). Uses precomputed fixture embeddings. |
| `tests/integration/test_vector_router_live_recall.py` | NEW (C.2b) — live smoke test for full pipeline including rerank. Skip-if-Ollama-unavailable. NOT load-bearing. |
| `scripts/generate_baseline_recall_fixtures.py` | NEW (C.2b helper) — one-time script to generate the fixture embeddings + critical evidence ref sets from bdde417 baseline outputs |
| `.env` + `.env.example` | MODIFIED (C.4) — `VECTOR_ROUTER_MODE` (disabled / shadow / narrow_only); `skip_enforce` DEFERRED, not exposed |

**Tests (per phase):**
- C.1: vector_search filter unit + arcadedb_schema sync
- C.2: build idempotency, cleanup scope, embedding dim
- C.3: endpoint shape, retrieve+rerank correctness, **reranker error → mode=full** (NOT vector-only — rev 8 M6), empty-after-threshold → mode=would_skip OR mode=full per fallback_to_full, malformed query → 422, `chunk_text → content_text` mapping for reranker
- C.4: dispatcher integration with HTTP mocked, **`would_skip` in narrow_only → dispatch RUN_FULL + persist `fail_open_reason`** (rev 9 M6 — replaces "skip writes terminal row"; that test moved to deferred `skip_enforce` future plan), `chunk_scope` flows as Celery kwarg + applied inside per-pass task via `apply_chunk_scope`, terminalize triggers cleanup, janitor scope, cross-DB janitor algorithm (ArcadeDB chunks + Postgres run statuses)
- C.5: RetrievalProfile range validation, manifest load with retrieval blocks
- C.6/7/8: live regression runs

**Deferred (post-VR, not blocking ship):**
- C4a (identity roster persistence) — only if a specific pass needs alias-augmented queries
- Per-pass embeddings cache reuse from the existing TextChunk vertices (the granularity-mismatch problem; would need an element_uids mapping layer)
- Embedding model fine-tuning on domain corpus
- Hybrid lexical-filter + vector-rerank (only if a quality regression on equipment-designations surfaces)

---

## C3 — Pre-pass skip router (Phase 3)

> ⚠️ **DO NOT IMPLEMENT — SUPERSEDED by VR section above.** Lexical implementation shipped as `db1ea53` and was reverted as `c16d3e7` on rev 5 after the user opted for vector retrieval + reranker. Rev 10 M8: section body collapsed into `<details>` to prevent agentic workers from acting on the historical checkboxes.

<details>
<summary><i>Historical C3 content (do not implement — click to expand)</i></summary>

**Goal:** Cheap deterministic router that examines anchors, markdown text, table captions, picture captions, and identity aliases per pass and emits `RUN_FULL` / `RUN_SLICED` / `SKIP_NO_EVIDENCE`. In `derive_ontology_graph`, synthesize `SKIPPED` StageRuns for optional passes when router says skip.

**Why:** Small / off-topic docs currently burn full LLM passes on numeric/spec fields they have no evidence for. The router's cue lists are manifest-driven (e.g. `radar_power_rf` cues: RF/power/frequency/band/radar-unit). The cue-list infrastructure is reused by C4 for chunk narrowing.

**Files:**
- Create: `app/services/extraction_routing.py` (the router)
- Modify: `app/services/ontology_bundles.py` (`BundleManifest` — add optional `cues: list[str]` field per pass)
- Modify: `app/workers/pipeline.py:derive_ontology_graph` (invoke router before each optional-pass dispatch)
- Modify: both manifest.yaml files (add `cues:` per optional pass)
- Test: `tests/unit/test_extraction_routing.py` (router unit tests)
- Test: `tests/unit/test_derive_ontology_graph_skip_router.py` (integration — skip synthesis writes SKIPPED stage_run)

**Acceptance:**
- Router input: list of document anchors, markdown text, table captions, picture captions, identity aliases (from cross-pass state when available).
- Router output per pass: `RUN_FULL` (default), `RUN_SLICED` (only used after C4), or `SKIP_NO_EVIDENCE`.
- **SKIP path writes BOTH a stage_run AND a terminal `pipeline_pass_outputs` row** with `execution_status='SKIPPED'`, `skip_reason='NO_RELEVANT_EVIDENCE'`, empty `extract_pass_response_json` + `diagnostics_json` carrying the router decision rationale. Then calls `mark_phase_terminal(...result='skipped')` and `_try_advance_phase(...)`. This is non-negotiable: phase advancement counts terminal `pipeline_pass_outputs` rows (`count_terminal_passes`), not stage_runs — writing only a stage_run would stall fan-in. (Per review finding 2.)
- Required passes (e.g. `system_links`) are NEVER skipped by the router — only optional ones.
- Status API surfaces `skip_reason='NO_RELEVANT_EVIDENCE'` for skipped passes.
- Acceptance harness 33/0 holds (it loads from fixtures, so router doesn't affect it directly — but Dvina is the live canary against over-aggressive skips).
- Shadow mode (per review finding 8): when `DOCLING_GRAPH_SKIP_ROUTER_MODE=shadow`, the router logs its decision but always returns RUN_FULL. Lets us compare a router-on shadow run vs a baseline run to validate decisions before enforcement.

**Open scope decisions (Task 1 — discussion gate):**
- (a) Are cue lists per-pass in the manifest, or per-entity-type in the ontology, or a separate cue-config file? Recommend: per-pass in the manifest (locality of reference).
- (b) Should the router be MANIFEST-DRIVEN with cue strings (substring match) or also support regex / token-class rules? Recommend: substring match first; regex deferred until needed.
- (c) Three-mode env var `DOCLING_GRAPH_SKIP_ROUTER_MODE`: `disabled` (no router invocation), `shadow` (decide + log but always RUN_FULL), `enforce` (decide + act). Mirror to .env / .env.example per [[feedback-env-vars-must-appear-in-dotenv-files]]. Default: `shadow` on first deploy, flip to `enforce` only after a clean shadow A/B.
- (d) Dvina canary — how do we know if we've over-skipped Dvina? Recommend: Dvina baseline shows full 5 passes ran; any pass that doesn't run on Dvina in shadow mode is a flagged warning, in enforce mode is a regression.

**Tasks:**
- [ ] **Task 3.1: Discussion gate.**
- [ ] **Task 3.2: Design the router signature** — `route_pass(pass_def, doc_metadata) -> RouteDecision`. RouteDecision is a small dataclass: `mode: Literal["RUN_FULL","RUN_SLICED","SKIP_NO_EVIDENCE"]`, `reason: str`, `matched_cues: list[str]`.
- [ ] **Task 3.3: Write failing router unit tests** — cues present → RUN_FULL; cues absent → SKIP_NO_EVIDENCE; required pass + cues absent → RUN_FULL (never skip required); empty cue list → RUN_FULL (no cue config = always run).
- [ ] **Task 3.4: Implement `app/services/extraction_routing.py`** — pure function, manifest-driven, no LLM calls.
- [ ] **Task 3.5: Extend `BundleManifest`** with optional `cues: list[str]` per pass; backward-compat default `[]`.
- [ ] **Task 3.6: Write failing integration test** — three sub-cases:
  (a) router decides SKIP and mode=enforce → `derive_ontology_graph_pass.delay` NOT called; SKIPPED stage_run + terminal pipeline_pass_outputs row written; phase marked terminal; `_try_advance_phase` called.
  (b) router decides SKIP and mode=shadow → log emitted at INFO with the decision; `derive_ontology_graph_pass.delay` STILL called (shadow only logs).
  (c) required pass + cues absent → router returns RUN_FULL regardless of mode (required passes never skipped).
- [ ] **Task 3.7: Wire router into `_claim_and_dispatch_pass`** (not `derive_ontology_graph` directly, per review finding 2 — that function only handles the initial wave; subsequent passes get dispatched via `_try_advance_phase` → `_claim_and_dispatch_pass`, which is the single chokepoint refactored in C1.6 Task 1.6.7). The router check runs INSIDE `_claim_and_dispatch_pass` before the Celery `.delay()`. On SKIP-enforce, write terminal pipeline_pass_outputs row + mark_phase_terminal + (re-enter) `_try_advance_phase` so the next-eligible pass dispatches. On SKIP-shadow, log only and proceed with normal dispatch.
- [ ] **Task 3.7b: Implement the terminal-skip write helper** — `_write_terminal_skip_row(db, run_id, pass_name, skip_reason, router_diagnostics)` that calls `save_pass_output` with execution_status=SKIPPED + empty payload + diagnostics carrying the router decision, then marks phase + advances. Must mirror the existing SKIPPED path used by `_execute_pass_attempt`'s "NO_UPSTREAM_ENDPOINTS" case (`_save_terminal_pass_output` already handles the SKIPPED execution_status correctly per the C0 work).
- [ ] **Task 3.7c: Write failing test for second-wave router enforcement** — `tests/unit/test_skip_router_second_wave.py`: arrange a 3-pass manifest (identity1, identity2, field_group) where identity1 finishes first, triggering `_try_advance_phase` → `_claim_and_dispatch_pass(field_group)`. Mock router to return SKIP_NO_EVIDENCE for the field_group. Assert (a) field_group is NOT queued, (b) its terminal SKIPPED row is written, (c) `_try_advance_phase` advances past it. (Regression test for the rev-2 bug where only the initial wave was routable.)
- [ ] **Task 3.8: Add `cues:` lists** to both manifest.yaml files. Initial cues per the user's plan: `radar_power_rf` (RF/power/frequency/band/radar-unit), `missile_kinematics` (range/speed/altitude/guidance/envelope/booster), `missile_propulsion` (engine/propellant/burn/thrust/stage). For the subset bundle: only `radar_power_rf` + `missile_kinematics` are present, so only those get cues.
- [ ] **Task 3.9: Add env var `DOCLING_GRAPH_SKIP_ROUTER_MODE`** (values: `disabled`, `shadow`, `enforce`; default `shadow`). Mirror to .env / .env.example.
- [ ] **Task 3.10: Run unit tests + acceptance harness** — expect GREEN + 33/0.
- [ ] **Task 3.11: Shadow-mode regression run** (mode=shadow). Cadence steps 1–6. Goal: verify the router's decisions are sensible without acting on them. Compare logged decisions vs which passes SHOULD have run per the bdde417 baseline (all 5 ran with HIT). Any "would-skip" decision on a pass that produced HIT entities = router cue list needs widening.
- [ ] **Task 3.12: If shadow looks clean, flip to enforce + second regression run.** Wall-time target: SA-2 ≤ baseline (router shouldn't add wall — every skip is pure win). Quality: all 10 gates hold; Dvina passes still HIT (canary against over-aggressive skipping).
- [ ] **Task 3.13: Optional third run with `mode=disabled`** for direct A/B against the shadow + enforce numbers if the user wants the 3-way comparison.
- [ ] **Task 3.14: Commit** as `feat(walltime): C3 — pre-pass skip router with shadow/enforce modes`.
- [ ] **Task 3.15: Report results** + promote/rollback decision before opening C4.

**Estimated effort:** Code + tests ~3.5h. Two regression runs (shadow + enforce) ~6h; optional third (disabled) ~3h.

</details>

---

## C4 — Identity-first chunk narrowing (Phase 4) ★largest expected gain★

> ⚠️ **DO NOT IMPLEMENT — SUPERSEDED by VR section above.** Lexical chunk-scope selection (C4c) and identity roster persistence (C4a) replaced by vector retrieval against ephemeral `ExtractionChunk` + cross-encoder rerank. C4b shipped as C1.6 (`84f6b12` + `9bc6cad` + `5c9572b`). C4d rolled into VR Phase C.4 worker wiring. Rev 10 M8: section body collapsed into `<details>` to prevent agentic workers from acting on the historical checkboxes.

<details>
<summary><i>Historical C4 content (do not implement — click to expand)</i></summary>




**Goal:** Run identity passes first (`radar_identity` + `missile_identity`), build an entity alias roster, then send only top-K relevant chunks to each field-group pass — not the whole document. Whole-doc fallback only when no candidates found AND the pass is important.

**Why:** From the user's original plan: "Large SA-2 style docs should drop field-group batch counts from ~50-60 to a much smaller number." Architecturally the biggest payoff in the entire wall-time plan. Reuses C3's cue-list infrastructure.

**Top-level acceptance for the combined C4:**
- Field-group passes (`radar_power_rf`, `missile_kinematics`) on SA-2 produce ≤ 50% of baseline `batch_count` when chunk_scope narrows the doc.
- Identity recall on SA-2 stable (23 + 44 entities = matches gates #3 + #7).
- Field fill counts on SA-2 stable (gate #5 21/34 emitter_function; gate #6 9/44 min_altitude_km).
- Dvina extraction unchanged.
- field_provenance evidence_id round-trips through scope back to original self_refs.
- Acceptance harness 33/0 + all 10 quality gates hold.

**Top-level open scope decisions (must be resolved before C4a starts):**
- Top-K — `K = min(manifest.top_k or 20, all_matching_chunks)`.
- No-match fallback — RUN_FULL when `fallback_to_full: true` in manifest; SKIP via C3 mechanism otherwise.
- Embeddings deferred (lexical-only here).
- Roster persisted, NOT in-memory (per review finding 1 — see constraint below).
- Serial identity → field-group → system_links is the new ordering. Loses some parallelism; field-group passes get so much smaller that net parallelism becomes irrelevant.

**Critical architectural constraint (forced by review finding 1):** `derive_ontology_graph` is a **dispatcher only** — it queues per-pass Celery tasks and returns. Per-pass workers run separately, so the alias roster CANNOT be an in-memory dict passed via worker state. The roster must be PERSISTED, and the dispatcher's phase advancement state machine (`_try_advance_phase`) must wait for identity passes to complete BEFORE field-group passes are queued.

**Critical service-side constraint (forced by review finding 4):** Docling refs are index-based (`#/texts/N`, `#/tables/N`). Dropping array items breaks every higher-index ref. The C0-era `_sanitize_docling_document` preserves array positions by BLANKING text (`text=""`, `orig=""`) — not removing items. The scoped DoclingDocument builder MUST follow this pattern: preserve original `texts[]` / `tables[]` arrays + only rewrite `body.children` to traverse a narrowed subset.

### C4 refactored into 4 sub-checkpoints

Each sub-checkpoint is committed and regression-tested independently.

#### C4a — Identity roster persistence

**Goal:** Add a compact identity-roster JSONB column + helper that an identity pass's terminal write populates from `pass_result.template_instance`. Field-group passes rehydrate the roster from the DB instead of in-memory state.

**Files:**
- Migration: `alembic/versions/0022_identity_roster.py` (new — adds `identity_roster_json` JSONB on `pipeline_pass_outputs`, OR a separate side-table if shape requires it; decide at discussion gate).
- Create: `app/services/identity_roster.py` — `build_roster_from_pass_result(pass_result, pass_def) -> list[RosterEntry]`, `merge_rosters(...)`, `load_run_roster(db, run_id) -> CombinedRoster`.
- Modify: `app/workers/pipeline.py:_save_terminal_pass_output` — populate `identity_roster_json` for identity passes only.
- Test: `tests/unit/test_identity_roster_extraction.py`, `tests/unit/test_identity_roster_persistence.py`.

**Acceptance:**
- Roster entry shape (review finding 7 — match `_extend_upstream_refs` semantics): `{entity_type: str, ref_id: str, identity_values: dict[str, Any], display_label: str | None, aliases: list[str]}`. `identity_values` is the ontology-driven identity field dict (matches `LogicalIdentity.identity_values_dict` shape in `extraction_merge.py:68`), NOT just `system_name`. `display_label` is computed by the existing `_compute_display_label(identity_values)` helper (`extraction_merge.py:452`). Aliases are the union of name/nomenclature/dieqp fields per the existing alias-collection pattern in the acceptance harness.
- `build_roster_from_pass_result(pass_result, pass_def)` reuses `logical_identity_from_dict` (existing helper) so the round-trip through `_extend_upstream_refs` is byte-faithful: a roster entry serialized and rehydrated yields an `upstream_refs[ref_id]` identical to what the in-process path produces today.
- `load_run_roster` merges all identity-pass rosters within a run into a deduplicated alias map keyed by canonical `LogicalIdentity` (not by raw `system_name`).
- All 10 quality gates hold (write-only change, no extraction semantics affected).

**Tasks (~10):** discussion gate → migration → roster module → wire into terminal write → tests → regression run.

#### C4b — Identity-first phase scheduling

**Goal:** `_try_advance_phase` ensures field-group passes are NOT dispatched until identity passes have terminalized. Cross-process synchronization via the same phase state machine that already powers `system_links.depends_on`.

**Files:**
- Modify: `app/services/run_phase_dispatch.py` (or wherever phase ordering lives — discovery is Task C4b.2).
- Modify: `app/workers/pipeline.py:derive_ontology_graph` — initial dispatch queues only identity passes; field-group + system_links deferred to per-pass terminal-write `_try_advance_phase` callbacks.
- Test: `tests/unit/test_phase_advance_identity_first.py` — with only identity passes terminalized, advance dispatches field-group; with identity in-flight, does not.

**Acceptance:**
- Field-group passes do NOT appear in Celery queue before their corresponding identity pass terminalizes COMPLETE.
- End-to-end graph_only ingest still completes.
- All 10 quality gates hold.

**Open scope decision (C4b discussion gate):** When an identity pass FAILS / SKIPS, do field-group passes still run (RUN_FULL) or also SKIP? Recommend RUN_FULL — preserves existing behavior; the C3 router separately handles "no evidence" skips.

**Tasks (~10):** discussion gate → map current ordering → write failing test → re-order dispatch → wire callback → tests → regression run.

#### C4c — Chunk-scope selection

**Goal:** Given a persisted roster + cue list, select top-K relevant chunks per field-group pass. Lexical-only.

**Files:**
- Create: `app/services/chunk_selection.py` — `select_chunks(doc, roster, cues, pass_def) -> ChunkScope`.
- Modify: `app/workers/pipeline.py:_build_extract_pass_request` — accept `chunk_scope: ChunkScope | None`, propagate to request body for narrowing-eligible passes.
- Modify: both manifest files — `narrowing_eligible: bool`, `fallback_to_full: bool`, optional `top_k: int` per field-group pass.
- Modify: `docker/docling-graph/app/schemas.py` — `ChunkScope` model on `ExtractPassRequest`.
- Test: `tests/unit/test_chunk_selection.py`.

**Acceptance:**
- Top-K dynamic: `K = min(manifest.top_k or 20, all_matching_chunks)`.
- No matches AND `fallback_to_full=true` → `ChunkScope(mode="full")`.
- No matches AND `fallback_to_full=false` → terminal SKIPPED row via the C3 skip mechanism.
- Chunks recorded with their original `self_ref` (no remapping at this stage).
- All 10 quality gates hold.

**Tasks (~8):** discussion gate → schema → selector tests → selector implementation → request-builder threading → manifest updates → tests → regression run.

#### C4d — Service-side scoped DoclingDocument with validated ref preservation

**Goal:** When `/extract-pass` receives a `chunk_scope`, build a doc whose `body.children` traversal is narrowed to the listed refs WHILE preserving original `texts[]` / `tables[]` / `pictures[]` arrays so index-refs remain valid.

**Files:**
- Modify: `docker/docling-graph/app/main.py` — new private `_apply_chunk_scope_to_doc(doc, chunk_scope)` applied before sanitize/normalize.
- Test: `docker/docling-graph/tests/test_chunk_scope_preserves_refs.py` — 50 texts + 5 tables, scope to `[#/texts/12, #/tables/1]`: arrays still 50 + 5, body.children only those 2, loads via DoclingDocument pydantic model without ref-resolution errors.
- Test: `docker/docling-graph/tests/test_chunk_scope_field_provenance.py` — `field_provenance.evidence_id` round-trips through scope back to original self_refs.

**Acceptance:**
- Original arrays preserved byte-for-byte. Only `body.children` (and any group `children`) rewritten.
- **Ancestor/parent-ref consistency validated** (review finding 4): for every item retained in the scoped traversal, its `parent` ref MUST be reachable from `body.children` via the rewritten traversal. If a retained text/table/picture's `parent` is a group/picture that's NOT in body.children, EITHER the parent container is also retained, OR the item's `parent` is rewritten to point at `body` (or a synthetic top-level container). Validation runs post-scope and fails the request with 400 if any ref is unreachable.
- DoclingDocument validates via pydantic post-scope (no `RefItem` resolution errors).
- field_provenance evidence_id resolves in original chunk space.
- All 10 quality gates hold; field-fill floors (gate #5 21/34, gate #6 9/44) verified explicitly because these are most exposed to chunk-narrowing bugs.

**Tasks (~10):** discussion gate → failing ref-preservation test → implement `_apply_chunk_scope_to_doc` → failing **parent-ref reachability test** (new — assert every retained item's parent resolves through body.children OR is rewritten to body) → failing field-provenance round-trip test → wire into extract_pass → wire validation step → tests → regression run.

#### C4 — Combined headline regression + commit

- [ ] **Task 4.combined: Combined regression run** after C4a + C4b + C4c + C4d all green. Headline target: SA-2 wall reduction ≥ 30% on field-group passes. Quality gates: all 10 must hold; explicit verification of gates #5 + #6 because they are most sensitive to narrowing.
- [ ] **Task 4.report: Report cumulative wall-time delta** + roster size for SA-2 + Dvina + any field-provenance round-trip failures. Promote / rollback per individual sub-checkpoint or as a bundle.

</details>

---

## C5 — Cache prepared inputs (Phase 5)

> ⚠️ **REVIEW REQUIRED POST-VR — REMOVED FROM ACTIVE CHAIN (rev 8 L10).** The original C5 design assumes:
> (a) chunk_scope is produced by the manifest (it's now produced by the VR router);
> (b) Layer B cache keyed on `chunk_scope_hash` (the hash semantics change because chunk_scope is now per-pipeline_run, not per-manifest);
> (c) docling-graph applies the scope (rev 8: scope is now applied **worker-side** by `apply_chunk_scope`, then the scoped doc is sent in the /extract-pass payload).
>
> **Layer A** (full sanitized base doc, doc-keyed) is still correct — that cache layer is scope-independent.
> **Layer B** (pass-specific) needs re-evaluation: what should the cache key be when chunk_scope is router-produced rather than manifest-declared?
>
> **Action:** do NOT implement C5 until VR ships C.8 and is verified in production. Re-evaluate the cache design against the new payload + prep profile, then either update C5 in place or write a new C5 plan.
>
> The rest of this section is preserved as historical reference but is NOT active.


**Goal:** docling-graph caches prepared artifacts across the multiple `/extract-pass` calls a single pipeline_run makes. Split into two cache LAYERS because preparation is partly pass-agnostic and partly pass-specific (per review finding 5).

**Why:** From the user's plan: "Repeated per-pass prep time drops near zero after first pass." With C0 telemetry, we can directly measure `sanitize_ms + table_normalization_ms + table_overlay_ms` per pass — that sum is the win.

**Two-layer cache shape (review finding 5):**

- **Layer A — Global (pass-agnostic, scope-independent).** Key: `(document_id, content_hash)`. Caches the FULL sanitized base doc:
  - sanitized DoclingDocument JSON (input to ALL passes, ANY scope) — pre-scope, full arrays
  - table-overlay parser result (independent of pass + scope; same overlay for every pass on the same doc)
  - per-doc evidence text buffer used by service identity filter

  **Ordering with C4 chunk_scope** (review finding 6): chunk_scope from C4d is applied AFTER the Layer A cache hit. Layer A's key intentionally does NOT include `chunk_scope_hash` — that would leak scope into a key that's supposed to be scope-independent. The flow on every `/extract-pass` is: Layer A lookup → (cache miss → sanitize + overlay + populate) → apply chunk_scope to the cached doc → continue to Layer B.

- **Layer B — Pass-specific.** Key: `(document_id, content_hash, pass_name, chunk_scope_hash, normalization_config_hash)`. Caches:
  - normalized table chunks (depends on `pass_name` because `_norm_skipped_for_pass` and table-relevance differ)
  - rendered TextItems from per-pass normalization (depends on `unit_convention`, `emit_unit_hint`)
  - chunk metadata + per-pass evidence units

These are SEPARATE caches in `_doc_cache.py`; misses on Layer B don't invalidate Layer A.

**Files:**
- Create: `docker/docling-graph/app/_doc_cache.py` — two cache classes, `GlobalDocCache` + `PassPrepCache`, both LRU + TTL.
- Modify: `docker/docling-graph/app/main.py:run_extraction_pass` — check global cache before sanitize + overlay; check pass-specific cache before normalize/render.
- Test: `docker/docling-graph/tests/test_doc_cache.py` — both cache classes; hit/miss; LRU; TTL; key-collision handling.
- Test: `docker/docling-graph/tests/test_extract_pass_reuses_cache.py` — pass 1 builds both caches; passes 2–5 on the same doc hit Layer A; passes 2–5 with the SAME `pass_name` hit Layer B; pass 1 of a different `pass_name` misses Layer B but still hits Layer A.

**Acceptance (review finding 5 — split per-layer):**

**Layer A (cross-pass, scope-independent) — applies whenever ≥2 passes touch the same `(document_id, content_hash)`:**
- Passes 2-N: `sanitize_ms < 5ms` AND `table_overlay_ms < 5ms`. (Layer A hit; `table_normalization_ms` is NOT in this target because Layer B owns it and DIFFERENT pass_names will miss Layer B by design.)

**Layer B (same-pass retries with the same scope) — applies when a pass retries on the SAME `(document_id, content_hash, pass_name, chunk_scope_hash, normalization_config_hash)`:**
- Retries 2-N of the SAME pass: ALL of `sanitize_ms < 5ms`, `table_overlay_ms < 5ms`, `table_normalization_ms < 5ms`.

**Both layers:**
- Cache evicts on TTL (default 1h) AND on content-hash mismatch.
- Memory ceilings: `DOCLING_GRAPH_DOC_CACHE_MAX_ENTRIES_A` + `DOCLING_GRAPH_DOC_CACHE_MAX_ENTRIES_B` (default 16 each; separate names to make per-layer tuning explicit).
- Acceptance harness 33/0 + 10 quality gates hold.
- Cache correctness verified by acceptance harness (same fixtures must produce same output post-cache; a stale Layer B entry returning wrong content for a new scope would corrupt extraction — caught by the 33 tests).

**Open scope decisions (Task 1 — discussion gate):**
- (a) Near-term in-process cache (per the user's plan) vs the better "send only document_id, service loads from MinIO" version? Recommend: near-term in-process this checkpoint; MinIO-side artifact key deferred.
- (b) Cache eviction policy — TTL only / LRU only / both? Recommend: both. LRU primary, TTL backstop.
- (c) `chunk_scope_hash` computation — hash of (sorted_self_refs + mode). Stable across runs.
- (d) `normalization_config_hash` computation — hash of (pass_name + token_limits + unit_convention + emit_unit_hint). Stable across runs.
- (e) On content-hash collision between two different runs: treat as cache hit (caller can't tell; content is byte-identical by construction).

**Tasks:**
- [ ] **Task 5.1: Discussion gate.**
- [ ] **Task 5.2: Design `DocCache` interface** — `get(document_id, content_hash) -> PreparedDoc | None`, `put(document_id, content_hash, prepared_doc)`. LRU + TTL.
- [ ] **Task 5.3: Write failing cache unit tests** — get-miss returns None, put-then-get returns the prepared doc, LRU evicts the oldest, TTL evicts past the deadline.
- [ ] **Task 5.4: Implement `_doc_cache.py`.**
- [ ] **Task 5.5: Write failing integration test** — second extract-pass on same doc skips sanitize+normalize+overlay (assert via the C0 timing metrics).
- [ ] **Task 5.6: Wire cache into `run_extraction_pass` + `extract_pass`** — compute content_hash from `body.docling_document_json`, check cache, prep on miss, return cached on hit.
- [ ] **Task 5.7: Add env var `DOCLING_GRAPH_DOC_CACHE_MAX_ENTRIES`** + mirror to .env / .env.example.
- [ ] **Task 5.8: Run unit tests + acceptance harness** — GREEN + 33/0.
- [ ] **Task 5.9: A/B regression run** — measure `sanitize_ms + table_normalization_ms + table_overlay_ms` sum across passes; subsequent passes should be ~0.
- [ ] **Task 5.10: Commit** as `feat(walltime): C5 — in-process per-doc artifact cache`.
- [ ] **Task 5.11: Full regression run** per the cadence. Wall-time targets (split per the corrected acceptance above):
  - **Layer A (cross-pass)**: passes 2-N on the SAME doc but DIFFERENT pass_names → `sanitize_ms + table_overlay_ms < 5ms` (Layer A hit; Layer B miss expected).
  - **Layer B (same-pass retry)**: same pass retried with the same scope → ALSO `table_normalization_ms < 5ms` on retry 2+.
  - Total sum is NOT the target. The C5 win is `sanitize + overlay` on every cross-pass run (5 passes × that delta) plus the bonus on any retries.
  - Quality gates: all 10 must hold; cache-invalidation correctness verified by acceptance harness.
- [ ] **Task 5.12: Report results** + promote/rollback decision before opening C6.

---

## C6 — Merge / import cleanup (Phase 7)

> ⚠️ **REVIEW REQUIRED POST-VR — REMOVED FROM ACTIVE CHAIN (rev 9 L10).** C6's original design persisted an `upstream_catalog_json` via a catalog serializer that was co-hosted with C4a's identity roster helper. With **C4a deferred indefinitely** (rev 5 Q4 — schema-only queries replace roster-augmented queries), C6 has no catalog serializer to depend on.
>
> **Options when re-evaluating post-VR:**
> (a) Un-defer C4a as a C6 prerequisite (rebuilds the roster helper for catalog use);
> (b) C6 builds its own catalog serializer in `_save_terminal_pass_output` independent of C4a;
> (c) Skip the catalog optimization entirely and address rehydration via the `@lru_cache` on `load_bundle_manifest` only.
>
> **Action:** do NOT implement C6 until VR ships C.8 and is verified in production. The rehydration share of `system_links` wall is the only part of C6 the VR rollout doesn't already address — and VR's wall-time win on the LLM share may make C6's catalog optimization marginal.
>
> The rest of this section is preserved as historical reference but is NOT active.


**Goal:** After LLM time is reduced by C2–C5, the worker-side fan-in (especially `system_links` rehydration) becomes measurable. Persist compact upstream entity catalogs so `system_links` doesn't re-parse every upstream `extract_pass_response_json`; `lru_cache` worker-side manifest loads; prefer provenance-derived chunk links over O(entities × chunks) substring scans.

**Why:** From the user's plan: "Merge/import time is measurable and no longer dominated by reparsing large JSON blobs." Note (per review finding 7): the bdde417 baseline's `system_links` is 13m on SA-2, but most of that is the system_links pass's OWN LLM extraction work (batch_count=12), not rehydration. C6 targets the rehydration share specifically.

**Catalog source (per review finding 6):** `outcome.pass_result.upstream_refs` is populated ONLY for `document_plus_entity_refs` passes (system_links). Entity passes (radar_identity, missile_identity, etc.) don't have `upstream_refs`. The catalog for entity passes must be derived from their `pass_result.template_instance` directly via a new serializer that walks the primary entity list per `pass_def.primary_entity_types`. The C4a `identity_roster.py` module can host this — C4a is a prerequisite for C6 catalog work, OR a sibling shared helper.

**Files:**
- Migration: `alembic/versions/0023_upstream_catalog.py` (new JSONB column `upstream_catalog_json` on `pipeline_pass_outputs` — small: list of {entity_type, ref_id, identity_values, display_label}).
- Modify: `app/workers/pipeline.py:_save_terminal_pass_output` — populate `upstream_catalog_json` from the pass result via the C4a roster serializer (extended to non-identity entity passes).
- Modify: `app/workers/pipeline.py:_rehydrate_upstream_refs_from_persisted_passes` — prefer `upstream_catalog_json` when present; fall back to full-JSON parse with a one-time WARNING per pipeline_run.
- Modify: `app/services/ontology_bundles.py:load_bundle_manifest` — add `@lru_cache(maxsize=32)`.
- Modify: `app/services/extraction_merge.py` — batched edge creation where order-independent (substring-scan path conversion DEFERRED — see Task 6.10).
- Test: `tests/unit/test_lru_cached_manifest_loader.py`.
- Test: `tests/unit/test_upstream_catalog_rehydrate.py` (per-pass-type catalog extraction + rehydration skips full-JSON parse).
- Test: `tests/unit/test_rehydration_backward_compat.py` (catalog absent → fallback path works, WARNING emitted exactly once per run).

**Telemetry gate (per review finding 7) + persistence path (per rev-3 finding 8):**
- **Wall-time gate is `rehydration_ms` reduction ≥ 50%, NOT total `system_links` wall reduction.** Currently `rehydration_ms` is not a C0 metric.
- **Persistence path matters.** `_rehydrate_upstream_refs_from_persisted_passes` runs in the WORKER, BEFORE the `/extract-pass` HTTP call. Pass diagnostics today land in `pipeline_pass_outputs.diagnostics_json` via two routes: (a) service-side `_delta_trace` → response → C0's `_build_pass_diagnostics_dict` merge; (b) worker-side `worker_diagnostics` field on `PassAttemptOutcome` → same merge helper. **`rehydration_ms` MUST use route (b)** — the C0 `worker_diagnostics` path. Adding it to the service-side path would never see it.
- Task 6.2 captures the metric via route (b) explicitly.
- The total `system_links` pass wall is also reported as a sanity check, but no specific target there.

**Acceptance:**
- `rehydration_ms` on SA-2 drops by ≥ 50% (catalog hit vs full-JSON-parse miss).
- All 10 quality gates hold (no change to extraction output — only the path of computing it).
- Catalog backward compat: old rows without catalog fall through cleanly; WARNING logged once per pipeline_run.

**Open scope decisions (Task 1 — discussion gate):**
- (a) Catalog column on `pipeline_pass_outputs` vs separate table. Recommend: JSONB column (small payload, single read path).
- (b) Should C6's catalog work REPLACE or EXTEND the C4a `identity_roster_json`? Recommend: EXTEND — keep the C4a column for identity passes, add a separate `upstream_catalog_json` for ALL entity passes (broader scope; identity passes carry both for clarity).
- (c) lru_cache maxsize. Recommend: 32.
- (d) Should the rehydration path log the fallback WARNING ONCE per run, or every time the catalog is missing? Recommend: once per run, suppress subsequent.

**Tasks:**
- [ ] **Task 6.1: Discussion gate.**
- [ ] **Task 6.2: Add `rehydration_ms` via the C0 worker_diagnostics path** (review finding 8 — persistence). Wrap `_rehydrate_upstream_refs_from_persisted_passes` with a `time.perf_counter()` capture (matching the C0 pattern in `_execute_pass_attempt`). Stash the value on a new keyword on `caller_worker_diag` passed into `_execute_pass_attempt` from `derive_ontology_graph_pass`. The existing `_build_pass_diagnostics_dict` merges worker_diagnostics into the final row's `diagnostics_json`, so `rehydration_ms` lands without any service-side changes. Add a focused unit test asserting `rehydration_ms` appears in the persisted diagnostics_json for `document_plus_entity_refs` passes (system_links) and is absent for entity passes (which don't call rehydrate). Run a baseline regression to capture the current `rehydration_ms` value before any catalog work — this becomes the gate baseline.
- [ ] **Task 6.3: Design upstream catalog shape** — `{entity_type, ref_id, identity_values, display_label}` per entity. Source per pass type: identity passes via C4a roster; non-identity entity passes via a new serializer in `app/services/identity_roster.py` that walks `pass_result.template_instance` per `pass_def.primary_entity_types`.
- [ ] **Task 6.4: Write failing test** — `tests/unit/test_upstream_catalog_extraction.py`: catalog extraction works for entity passes (not just identity passes); raises a clear error for non-entity-pass shapes.
- [ ] **Task 6.5: Add migration `0023_upstream_catalog.py`** for `upstream_catalog_json` JSONB column on `pipeline_pass_outputs`.
- [ ] **Task 6.6: Populate catalog** in `_save_terminal_pass_output` — call the new serializer for entity passes (not document_plus_entity_refs passes).
- [ ] **Task 6.7: Write failing test** — `tests/unit/test_upstream_catalog_rehydrate.py`: rehydration uses catalog when present; falls back to full-JSON parse with a single per-run WARNING when absent.
- [ ] **Task 6.8: Update `_rehydrate_upstream_refs_from_persisted_passes`** — catalog-first; fallback path emits ONE WARNING per pipeline_run (use a Redis or per-process counter).
- [ ] **Task 6.9: Add `@lru_cache(maxsize=32)`** to `load_bundle_manifest`. Quick unit test asserting second load is cached.
- [ ] **Task 6.10: ~~Convert provenance-substring path~~** — **DEFERRED to a separate later checkpoint (per rev-3 finding 9).** Replacing substring-derived structural edges with `field_provenance.evidence_id` can change graph connectivity (MENTIONED_IN / EXTRACTED_FROM edges land differently when the source changes from substring scan to provenance lookup). That deserves its own checkpoint with retrieval/structure-link quality gates beyond the 10 we have. Out of C6 scope; documented as a follow-up.
- [ ] **Task 6.11: Run unit tests + acceptance harness** — GREEN + 33/0.
- [ ] **Task 6.12: Commit** as `feat(walltime): C6 — upstream catalog + merge cleanup`.
- [ ] **Task 6.13: Full regression run** per the cadence. **Wall-time gate: `rehydration_ms` on SA-2 drops by ≥ 50% vs the Task 6.2 baseline** (NOT total `system_links` wall). Quality gates: all 10 must hold; `system_links` composition (VARIANT_OF 15 / ASSOCIATED_WITH 11 / CUES 3) unchanged. Report total `system_links` wall as sanity-check context, not as the gate.
- [ ] **Task 6.14: Report results** + promote decision. After this, the plan is complete — proceed to Post-C6 cleanup.

---

## Deferred follow-ups (not in this plan)

- **Provenance-substring → field_provenance.evidence_id** (from rev-3 finding 9): replace O(entities × chunks) substring scans with provenance-derived chunk links. Can change graph connectivity (structural edge sources differ). Deserves its own plan with retrieval/structure-link quality gates beyond the 10 we have here. Track as a future plan when C2–C6 are done and the wall-time work has settled.
- **MinIO artifact-key cache** (deferred from C5): worker sends `(document_id, run_id, artifact_key)` to the service; service loads from MinIO once and shares across passes. Bigger payload-size win than C5's in-process cache, but requires changes to the worker → service request shape. Track as a future plan.

## Post-C6 cleanup (optional)

- [ ] **Task 7.1: Update [[extraction-baseline-bdde417]] memory** — record new wall-time floors achieved by C2–C6 + structural-zero gates still holding.
- [ ] **Task 7.2: Append final run-log entry** to `docs/sa2_extraction_runs.md` with the new wall-time figures.
- [ ] **Task 7.3: Merge `walltime/c0-telemetry` → main** (commits 3e01a0b → e823abd → C1.5 → C2 → C3 → C4 → C5 → C6).
- [ ] **Task 7.4: Decide on `air_defense_v3_baseline_subset` bundle longevity** — keep for future regression work (per [[regression-test-source]]), or delete now that C2–C6 are done.
- [ ] **Task 7.5: Decide on the docling-graph image's COPY of patched code** — if the worktree-derived image is the canonical one going forward, retag and push; otherwise rebuild from main.

---

## Risk-management checklist (applies to every checkpoint)

Before any code touch in a checkpoint:
- [ ] Discussion gate completed; user said proceed.
- [ ] Existing tests pass (run the C0+C1+P6 verification suite).
- [ ] Git working tree clean.

After code + tests pass:
- [ ] **Full regression run executed** — graph_only reingest on both baseline docs with the subset bundle. NON-NEGOTIABLE per the user's directive that every step concludes with a regression test.
- [ ] Acceptance harness 33/0 ✓ (rechecked post-rebuild if image changed).
- [ ] All structural-zero gates ✓.
- [ ] Per-pass entity counts ≥ baseline ✓.
- [ ] Field-fill floors ✓ (gates #5 emitter_function 21/34 + #6 min_altitude_km 9/44).
- [ ] Wall-time delta reported with C0 telemetry → user reviews before next checkpoint promote.

Before committing:
- [ ] No new env vars without `.env` + `.env.example` mirror ([[feedback-env-vars-must-appear-in-dotenv-files]]).
- [ ] No destructive operations attempted without user confirmation ([[feedback-confirm-before-destructive-actions]]).
- [ ] No production-code path uses the regression-only bundle by default (only via explicit source default or override).
- [ ] Commit message includes the verification result + the wall-time delta + any deferred-followup notes.
